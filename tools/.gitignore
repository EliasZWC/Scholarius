# 诊断脚本产出的临时文件
_ref_*.txt
__pycache__/

# ⚠️ 探针脚本的**全部**临时输出都忽略，不要逐个列举。
#
#    踩过的坑：原来只写 `_ref_*.txt`，结果 _gh.txt / _meta.txt / _v.txt /
#    _venues.txt 这些同样由探针产出的文件**不在忽略范围内** ——
#    它们未出现在 `git status` 的忽略列表里，一次 `git add -A`
#    就会连内容一起提交上去。
#
#    ⚠️ 而它们的内容是**从真实论文 PDF 里提取的正文**
#       （标题、Subject 元数据、引用段、会议名），
#       和下面的 _font_*.json 是同一类版权问题。
#       所以必须忽略 —— 不是"整洁"问题，是版权问题。
#
#    ⚠️ 规则用 `_*.txt` 而不是更精确的白名单：
#       探针脚本的名字是随手起的（_gh / _meta / _v / _venues），
#       以后还会加新的。逐个列举必然漏 —— 事实上已经漏了 4 个。
#       这些脚本本来就是"看一眼就删"的一次性工具，
#       输出没有任何需要长期保留的东西。
_*.txt

# 回归测试夹具。
# ⚠️ 不提交：内容是从真实论文里提取的**全文**，受版权保护。
#    由 tools/make_font_fixture.py 从本地 PDF 现场生成：
#        python tools/make_font_fixture.py "<论文.pdf>"
#    路径固定在 tools/smoke/_font_<年份>.json。
_font_*.json
_fixture_*.json
