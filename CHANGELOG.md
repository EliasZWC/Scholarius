# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [0.1.30] - 2026-09-25

### 新增「原始视图里直接选中 PDF 文字」

用户反馈（原话）：

> 1. 「依旧拖拽无法形成，我根本无法正常选中任何内容。」
> 2. 「不是，pdf 中的文本选中这么难吗？划分个 pdf 的区域这么难吗？」
> 3. 「不是画框也无所谓，字体根本无法选中啊」「都是只能识别点击」
> 4. 「面对任何形式的拖拽都没有办法识别啊」

#### 做了三件事

1. **原生侧按页吐文字盒** —— `PdfText.pageLines()`（新增），
   `WebAppBridge.getPdfPageLines(id, page)` 暴露给网页，
   `MainActivity.pdfPageLinesFor()` 序列化成 JSON。
   实测真实论文第 1/2/3 页分别 402 / 629 / 283 个词盒。

2. **网页侧铺一层透明文字层** —— `mountTextLayer()` 在每个页图上
   按归一化坐标摆一组 ``<span class="pdf-text-line">``，
   文字颜色透明（看不见）但可选中。
   字号**直接用行盒高度**（`y1 - y0`），不做磅值换算 ——
   避免引入"A4 宽 595pt"这类会跨版式出错的假设。

3. **手势自己接管** —— 这是最关键的一步，理由见下。

#### ⚠️ 为什么必须自己实现选字（真机实测）

`touch-action` 的取值是**整条祖先链的交集**。
链条里 `.reader-body` 是 `overflow-y: auto`，
只要有一环允许 `pan-y`，浏览器就把手指拖拽认领成**滚动**，
发 `pointercancel` 掐断 pointer 链 —— 事件根本收不全。

所以必须把整条链锁成 `touch-action: none`。
但锁成 `none` 之后，浏览器**连选字手势也不发起了**：

```
pointerdown @76,655  -> SPAN.pdf-text-line
pointermove ×16      -> SPAN.pdf-text-line   ← 事件全到达
pointerup @228,663   -> SPAN.pdf-text-line
滚动: 0 -> 0                                  ← touch-action 生效
选区: len=0                                   ← ❌ 浏览器不选
```

而 CSS 里**没有**"允许选择、不允许滚动"这个值。
→ 只能自己实现（PDF.js 也是这么做的）：

- `pointerdown` 记起点（词 + 词内偏移）
- `pointermove` 用 `selection.setBaseAndExtent()` 更新选区
- 纵向为主的拖拽 → 自己改 `.reader-body.scrollTop`（滚页面）

#### 两种拖拽的区分

| 手势 | 结果 |
|---|---|
| 横向为主 | 选中文字（`setBaseAndExtent`） |
| 纵向为主 | 滚页面（`scrollTop`，1:1 跟手） |
| 未过阈值 | 什么都不做（避免"点一下"误触） |

阈值按物理尺寸定（3mm ≈ 11.3 CSS px），与编辑模式的 `DRAG_SLOP` 同源 ——
真机手指"点一下"的天然抖动有 10~15px，写死 8px 会误判。

#### 顺带修掉的两个真 bug

- `showPdfScroll()` 在 `contentEl` / `currentDoc` 未就绪时提前 return，
  但 `view` 已经被改成 `'raw'` → **状态与 DOM 不一致**：
  界面上是阅读视图内容，`is-raw` 类却挂着，再点切换按钮会因
  `want === view` 直接 return，永远修不回来。

- `bindTextLayerPan` 的文档注释头在一轮编辑中被吃掉，只剩孤立的 `*`，
  导致整个 `reader.js` 抛 `SyntaxError: Unexpected token '*'`
  → `ScholariusReader` 从未定义 → **点击文献卡片毫无反应**。
  这个错误 `node --check` 也会报，但当时漏跑了。

#### 验证（真机模拟器，真实论文，真实触摸）

| 脚本 | 结果 |
|---|---|
| `tools/emu_select_check.py` | ✅ 层挂上 / 可选中 / 透明 / 画框模式不抢手势；拖拽选中 1017 字符 |
| `tools/emu_pan_check.py` | ✅ 纵向拖 -260px → `scrollTop` 精确 +260，且不误选字 |
| `tools/emu_lines_api.py` | ✅ 桥接口 402 / 629 / 283 行 |
| `tools/emu_class_check.py` | ✅ 编辑态类不残留（9/9） |
| `tools/emu_reflow_check.py` | ✅ 阅读视图按更改后的类型重排 |
| `tools/emu_delete_hit.py` | ✅ 删除键可达 |

---

## [0.1.29] - 2026-09-25

### 修复「编辑状态与视图不同步」+「标注不影响阅读视图排版」

用户反馈（原话）：

> 1. 「清除按钮可以清除框，但单独框点开表单选择清除依旧无法清除。」
> 2. 「无法拖拽形成框，相当于整个选中功能不可用；点击可用，但这是错误的
>      逻辑，因为单纯点击应该是唤出菜单，而不是形成一个框（且框的大小
>      无法确定）。」
> 3. 「阅读视图并没有按照更改后的框重新排版。」

#### ⚠️ 共同根源：编辑栏选项是「开关」，点了会取消，但没有可见反馈

`makeEditTab` 里选项按**开关**语义实现（用户要求：

> 「就是点击这个选项，再点击就可以取消这个选项」）——
再点已选中的会 `annotateMode = null`。

而 `null` 时浮层是 `pointer-events: none`、也不绑画框监听，
页面纯滚动。**用户以为还在矩形模式，实际已经是"没选任何类型"**
→ 拖拽毫无反应、点框行为错乱、且**没有任何提示**。

#### 问题 2：「无法拖拽形成框」

两个原因：

**① `annotateMode === null` 时无提示**

`null` 时页面纯滚动（这个行为本身是对的，用户明确要求
「哪个选项都没点，就不需要画框啊」），但缺"为什么画不出框"的反馈。

✅ 新增 `bindNoModeHint()`：`null` 时在浮层上绑一个**轻量**监听 ——
   只在"短促点击"（位移 < 12px 且 < 600ms）时弹一次提示
   `Pick An Option Below To Start.`；拖拽（想滚页面）不打扰。
   ⚠️ 不 `preventDefault`、不改 `touch-action` → 滚动不受影响。

**② `MIN` 太小 → 「点一下」被记成一个极小的框**

```
MIN = 0.012 是**归一化**值 → 412px 页宽上只相当于 4.9 CSS px
DRAG_SLOP = 11.3px
```

只要手指抖动超过 slop（约 12px 位移）就能产生 4.9px 以上的框，
于是"点一下"经常生成一个**大小莫名其妙的小框**。

✅ `MIN` 提到 `0.05`（≈ 20.6 CSS px）—— 真想画框时随手一拖都是
   几十像素，"点一下 + 抖动"到不了这个尺寸。
   真机验证：15px 拖拽被 `anno:reject | too-small w=0.0267 h=0.0188` 拒绝。

#### 问题 2 的另一半：「点击应该是唤出菜单，不是形成框」

✅ 矩形模式下**未超 slop 的点击**改为主动 `toggleMenu()`。

⚠️ 为什么不能靠 click 冒泡：`touch-action: none` 之下真机**不派发
   click**（见 `makeRegionBox` 的说明），`bodyEl` 的切菜单监听收不到。
   不主动唤起的话，矩形模式下点空白处**毫无反应**。
⚠️ 唤菜单前先 `markDragged()` —— 这一次 pointerup 之后浏览器仍可能
   补发 click（桌面/部分机型），不标记会**再切一次**，菜单闪一下又回去。

#### 问题 3：「阅读视图没有按更改后的框重新排版」

⚠️ 真机诊断出的真因（三个独立缺陷叠加）：

**① `b.textType` 全项目没有任何地方赋值**

```js
makeRegionEl:   var el = makeBlockEl(b, b.textType);   // 唯一读取处
b.textType = …  // 不存在
```

于是 `ttype` 永远是 `undefined`，`makeBlockEl` 只按 `b.kind`
（**原生判定**）决定标签与样式 → **用户标注对阅读视图毫无影响**。

✅ 在 `buildRegions` 算完 `type` / `level` 之后写回 `blk.textType`。

**② 位置判据覆盖了用户标注**

```js
if (!sawBody && !sawTitle && …) type = 'title';       // 无 !mark 守卫
if (!sawBody && authorPending && type === 'body') type = 'author';
```

实测：把首页的一段标成「章节标题」，被第一条判据强制改成 `title`
→ 归入首页区 → 走 `makeRegionHeader`（只输出文字、不读 textType）
→ 标注被彻底忽略。

✅ 两处都加 `!mark` 守卫 —— 用户表过态就不该被位置判据改写。

**③ `appendToStack` 没传 `level`**

```js
target.blocks.push({ kind, text, page, textType });   // 少了 level
```

`makeBlockEl` 里 `var lv = (b.level >= 3) ? 4 : …` 读到 `undefined`
→ 恒为 h2，用户标的 L2/L3 全部丢失。

✅ 补上 `level: blk.level`。

**真机验证**（用 `buildRegions` 的决策日志，最可靠）：

```
改前: blk48 | mark=null    type=body             → appendToStack（正文块）
改后: blk48 | mark=heading type=heading level=1  → openRegion（建标题区）✅
```

#### 问题 1 + 状态错乱：`setView()` 切视图时不退出编辑模式

真机抓到的矛盾状态：

```
readerClass   : "reader is-open is-menu-open is-annotating"
hasPageImg    : false     ← 不在原始视图
hasReaderPara : true      ← 在阅读视图
layers        : 0         ← **没有任何标注层**
pressed       : "formula" ← 编辑栏还选着矩形模式
```

`setAnnotating` 规定了「只有原始视图能进编辑模式」
（`if (next && view !== 'raw') return;`），但那条守卫**只在进入时检查
一次**。之后切视图没有联动 —— `teardownPdfScroll()` 把页图连同标注层
全部销毁，**但 `annotating` 仍是 true**、`is-annotating` 类还在、
编辑栏还挂着、`annotateMode` 还是 `formula`。

用户于是处于「看起来在编辑模式、但页面上没有任何可编辑的东西」的状态——
想画框没层、想改框没层、阅读视图也不反映标注。

✅ `setView()` 里离开原始视图前先 `setAnnotating(false)`
   （走存盘 + 恢复底栏 + 清类）。理由：进编辑模式本就要求原始视图，
   两个状态必须一致；用户切到阅读视图的意图是"看正文"。

#### 另外修掉

`setAnnotating(false)` 未清 `is-text-mode` 类 → 退出编辑模式后类残留。

#### 真机验证工具（新增/加固）

| 脚本 | 用途 |
|---|---|
| `tools/emu_reflow_check.py` | 改标注 → 切回阅读视图 → 验证**排版结构**真的变了 |
| `tools/emu_fix4_check.py` | 点击唤菜单 / 未选类型提示 / MIN 提高 / 切视图退出编辑 |
| `tools/emu_issue3_check.py` | 用户三个问题的逐步复现（观察用） |

⚠️ 全部做成**幂等**：无论应用处于阅读视图/原始视图/编辑模式/
   未打开阅读器，都能自己走到目标状态再断言。

#### ⚠️ 排查方法论教训（血亏几轮）

1. **验证脚本的断言本身可能错**：
   - 以为 `.anno-block` 含文本 → 它其实是**空定位框**（文字在页图位图里），
     `textContent` 只有标签文字
   - 以为"heading 元素数量变化"能证明重排 → 被改的块原本已是 heading，
     数量不变，**功能是好的却报失败**
   - 最后改用 `buildRegions` 的**决策日志**（`blkX` / `openX`）才可靠

2. **行号是"按累计行数估算"的**，与 DOM 的 `data-block-line`
   不一定一一对应 —— "按 line 去 DOM 找元素"本身就不可靠。

3. **诊断 trace 要打在"决策点"上**，而不是只打结果。
   本轮正是靠 `reader:blkX`（type 算完后）+ `reader:openX`（走哪个分支）
   两条日志，一次定位到"标注命中了但没影响渲染"。

---

## [0.1.28] - 2026-09-25

### 过滤重复重叠块 —— 一次修好「框是乱的 / 删不掉 / 越改越多」

用户反馈（原话）：
> 「自动识别的框是乱的」
> 「改变分类，结果是增加后，旧分类到了框的右下角，然后再改，又增加了一个」
> 「清除后我增加了一个框，然后就删不了了」
> 「并且也无法增加其他的框」

#### ⚠️ 关键：之前三版都复现不出来，因为用了**自造 PDF**

自造 PDF 每个文本块只有 1 行（每行重设 `Tm` → `PdfText` 切成单行块），
与真实论文结构差太远。改用**真实论文**（Transformer / Attention Is All
You Need）后**一次复现**：

```
可见框 45 个，其中 15 对是重叠的，多对 ratio = 1.0（完全包含）
```

#### 真因：原生输出的块里有大量**互相包含的重复块**

同一段视觉文本被输出成**两套块** —— 整块 + 内部每个词：

| 块 | 类型 | 位置 | 是什么 |
|---|---|---|---|
| `line=0` | heading | `[89,214,180,59]` | 标题整体 |
| `line=1` | heading | `[111,266,68,7]` | 词 "Attention" |
| `line=2` | heading | `[180,266,61,7]` | 词 "Is" |
| `line=3` | heading | `[242,266,61,7]` | 词 "All" |
| `line=4` | heading | `[94,266,241,14]` | 又一层 |

原因：`PdfText` 的 `startsNewBlock` 用**字号/粗体**判标题，而标题里
每个词本身也是大字粗体 → 每个词都独立成块；同时整行又被合成一个块。

#### 为什么这一条解释了**全部**现象

重叠框让「点击命中」变得不确定 —— **用户看到的是下层，点下去命中的是上层**：

| 用户操作 | 实际发生 | 用户看到 |
|---|---|---|
| 改分类 | 改了**被盖住的另一个块** | "旧分类跑到右下角"（那些词块正贴在标题右下） |
| 删除 | 删掉上层，用户看到的那个还在 | "**删不掉**" |
| 反复操作 | 越积越多 | "分类**最多三个**" |
| 继续操作 | 那块位置被上层框占满 | "**无法增加其他框**" |

#### 修复

新增 `dropContainedBlocks()`，在 `setText()` 里**渲染与缓存之前**
过滤掉「被别的块覆盖 ≥ 85%」的块。

⚠️ 必须在数据入口处过滤 —— 这样渲染 / 标注 / 点击用的是**同一份**
数据，不会出现"渲染算一套、点击算另一套"。

判据：
- 只在**同页**比较
- 覆盖率 = 交集面积 / 被检块面积，阈值 `0.85`
  （实测完全包含时 0.97~1.0，相邻但不同的块 < 0.6，有余量）
- 互相覆盖且面积相近时，只丢**行号更大**的 → 保证结果确定性，
  不依赖遍历顺序

**真机实测效果**（Transformer 论文第 1 页）：

```
重叠对  15 → 1
可见框  45 → 20
块     100 → 53
```

#### 同时修掉的另两个问题

**1. 「再清除 → 自动识别的乱框复活」**

`clearTextMarksByType(null)` 的第 1 步把上一轮"标成 body 的覆盖标注"
也一起丢了 → `effectiveTypeAt` 退回原生判定 → 乱框复活。

```
实测：① {heading:5, author:1}  →  ② {}  →  ③ {heading:5, author:1}
日志：两次都是 clear affected=3（第二次"清除"反而清出了 3 个东西）
```

修法：body 覆盖是**清除操作的状态**，不是用户的意图，清空时不能丢。

**2. 「清除后还剩一个无法改变的框」**

页图从 `y=0` 渲染，而顶栏占 `y=0~113` —— 论文页首标题块落在
`y=73~80`，**整块在顶栏底下**：

```
elementFromPoint(134, 77) === HEADER.reader-top
实测：6 个可见框里有 1 个 unreachable
```

修法：编辑模式下给 `.reader-body` 加 `padding-top: var(--reader-top-h)`。

⚠️ 只在编辑模式下推 —— 阅读模式保持"菜单是浮层、不占内容高度"
的原有设计（那条设计前提针对阅读，编辑模式要操作任意位置，
顶栏挡路是功能性缺陷）。

#### 附带修复

- `setAnnotating(false)` 未清 `is-text-mode` 类 → 退出编辑模式后类残留
- 新增顶层变量 `--reader-top-h`，让"顶栏高度"与"下推量"用同一份值
- `tools/make_test_pdf.py --paper` 改用 `T*` 连续排版，
  修掉"每行一个块"的失真

#### 新增真机验证工具

| 脚本 | 用途 |
|---|---|
| `tools/emu_realpaper_check.py` | 用真实论文走完整用户流程（逐步 dump） |
| `tools/emu_persist_check.py` | 标注保存 / 恢复 / 删除的持久化 |
| `tools/emu_clear_check.py` | 清除两次是否复活 |
| `tools/emu_afterclear_check.py` | 清除后新增框能否删除 |
| `tools/emu_box_tap.py` | 点框是否删除 |
| `tools/seed_emu_library.py --pdf=` | 塞入指定 PDF（真实论文测试） |

#### ⚠️ 教训

**必须用真实论文复现，不要自造 PDF。**

`tools/probe_meta.py` 里一直写着真实论文的路径，用假数据测真问题是
白费功夫 —— 自造 PDF 的结构失真（单行块）恰好把真因（重叠块）
掩盖掉了，导致连续三版都没修对。

---

## [0.1.27] - 2026-09-25

### 画框功能修复：真机实测找到三层真因（并建立本地模拟器验证）

用户连续三版（0.1.24/25/26）都反馈「无法删除框，无法拖拽建立新框」。
本版**在本地 Android 模拟器上复现并修好了**，同时把
「改完必须先在真机验证」变成能力而不是口号。

#### 为什么之前三版都没修好

之前所有验证都在**桌面 Chromium（Playwright）**里做，
而这三个 bug 全是 WebView 手势相关 —— **桌面复现不出来**，
所以每次本地测全绿、推给用户全废。

本版装了本机模拟器（JDK 17 + Android SDK + AVD），
并打通了 WebView 远程调试，于是能在**真 Android WebView** 里：

- 执行任意 JS（`tools/emu_js.py`）
- 派发**真实触摸事件**并记录完整事件序列（`tools/emu_touch.py`）

真实触摸是必须的：合成 `PointerEvent` 不走浏览器的内部
pointer 状态机，复现不出 `touch-action` 相关的吞并行为。

#### 真因（三层，缺一层都修不好）

**① 文字块抢走了 `pointerdown`**

```
.anno-block   z-index: 2   ← 一页几十个，几乎铺满页面
.anno-layer   z-index: 1
```

`.anno-block` 是 `.anno-layer` 的子元素且层级更高，所以
`elementFromPoint` 命中的是**块**而不是层。而 CSS 里有

```css
.reader.is-annotating .anno-block { pointer-events: auto; }
```

它在**所有**编辑模式下都让块接事件。于是拖拽起点只要碰到
文字块（大概率），浏览器就把它当成"在可滚动元素上拖动"。

**② `pointer-events` 的初始值就是 `auto` —— "不写" ≠ "none"**

第一轮我改成"只在文本模式给 auto"，结果仍然失败：
非文本模式下没写规则，块取默认值 `auto`，照样吞事件。
必须写**两条互斥规则**：

```css
.reader.is-annotating .anno-block         { pointer-events: none; }
.reader.is-annotating.is-text-mode .anno-block { pointer-events: auto; }
```

**③ `touch-action: manipulation` 允许 `pan-y` → 浏览器判成滚动**

`.anno-layer` 的祖先是 `.reader-body`（`overflow-y: auto`，可滚动）。
`manipulation` 展开是 `pan-x pan-y pinch-zoom`，**明确允许平移**。
于是浏览器开始滚动页面并发出 `pointercancel`，掐断整条指针链：

```
pointerdown   -> DIV.anno-layer.is-drawing
pointermove   -> DIV.anno-layer.is-drawing
pointercancel                    ← 链被掐断，框画不出来
```

⚠️ **0.1.26 的结论是误诊**。那一版认为"`touch-action: none`
会吞掉整条 pointer 链"，于是改用 `manipulation`。
实际当时真正的元凶是 ①，`none` 是被冤枉的 ——
改成 `manipulation` 反而制造了 ③。

正解就是 `touch-action: none`：把这一元素上的平移/缩放
全部收回给自己。页面仍可滚动 —— 在标注层以外的地方照常滑
（层只覆盖页图）。画框时不能滚页面，这正是我们要的语义。

#### 修复

| 文件 | 改动 |
|---|---|
| `styles.css` | `.anno-block` 改为**两条互斥规则**；`.is-drawing` 的 `touch-action` 改回 `none`（附推翻旧结论的说明） |
| `reader.js` | 新增 `syncModeClass()`，在 `mountAnnotateLayer()` **之前**同步 `is-text-mode` 类（顺序反了会有一帧漏洞） |

#### 真机回归结果（全部真实触摸）

```
A. 点一下不生成框          ✅   ← 用户报的「点击直接生成一个框」
B. 抖动不生成框            ✅
C. 拖拽生成 1 个框         ✅   ← 用户报的「无法拖拽建立新框」
D. 点框删除                ✅   ← 用户报的「无法删除框」
E. 文本模式文字块可点       ✅
F. 非编辑模式可滚动         ✅   （scrollTop 0 → 485.7）
```

#### 新增验证工具

| 脚本 | 用途 |
|---|---|
| `tools/emu_js.py` | 在真机 WebView 里执行 JS |
| `tools/emu_touch.py` | 派发真实触摸 + 记录事件序列 |
| `tools/emu_draw_check.py` | 画框 8 步带断言回归 |
| `tools/emu_regress.py` | 手势全量回归（A–F） |

#### ⚠️ 排查方法论（写进记忆）

- **判断元素能不能被点到，唯一可靠的方法是 `elementFromPoint`。**
  `getComputedStyle(el).pointerEvents` **不能**用来判断 ——
  未显式设置时它返回初始值 `auto`，**即使父层是 `none`**。
  这个坑让本轮多花了一轮重建。
- 回归脚本里按百分比取测试点不可靠：顶栏/底栏是浮层，
  必须逐点 `elementFromPoint` 验证落在目标元素上。

#### 文案修正

- `vault.hint.shortTitle`：`Shown instead of title` → `Shown Instead of Title`
  （是短语，应 Title Case；`of` 作为短介词保持小写）
- `tools/check-case.js` 补两条判据：
  - 「名词主语 + 助动词」也算句子（原来只认代词主语，
    漏掉 `Cards will show the full venue name again.`）
  - 白名单补 `of/in/on/at/by/to/for/from/with` 等标准 Title Case 里小写的短介词

---

## [0.1.26] - 2026-09-25

### 画框完全不能用：`touch-action: none` 吃掉了整个 pointer 链

用户实测（0.1.25）：「无法删除框，**无法拖拽建立新框**，
基本这个功能没用」。

#### 关键线索：连拖拽都建不了框

上一版（0.1.25）我只解决了「点框不删」，本地测全绿。
但用户说**拖拽也建不了框** —— 说明 pointer 事件根本
没到达监听器，不是某个判据写错。

#### 真因：`touch-action: none` 在 Android WebView 上会吞掉 pointer 事件

```css
.reader.is-annotating .anno-layer.is-drawing {
    touch-action: none;    /* ← 凶手 */
}
```

它本来是为了一件事：不给的话手指拖拽会被浏览器当成
"滚动页面"，`pointermove` 收不到几个点，框只画出一小段。

但在 Android WebView 上，部分版本**连 pointerdown/pointermove
一起不派发了** —— 不只是阻止滚动，是把整条 pointer 链都吞了。
而桌面 Chromium 没这个行为 → **本地测全绿、真机全废**。

✅ 修法：

- CSS 改成 `touch-action: manipulation`（只禁双击缩放，**不禁滚动**）
- 真正的"别滚动"由 **JS 在 pointerdown 里 `preventDefault()`** 完成 ——
  那是我们自己的代码，不受 WebView 的 touch-action 实现差异影响

#### ⚠️ 附带事故：诊断日志自己不出声

排查时我在 reader.js 里加了十几条 `trace('anno:xxx', ...)`，
结果日志里**一条都没有**。

原因：reader.js 写的是 `if (global.trace) global.trace(...)`，
而 app.js 只把 trace 挂在 `window.Scholarius` 上 ——
`window.trace` 是 undefined，于是所有诊断调用**静默跳过**。

✅ 把 `trace` 同时挂成 `window.trace`。

⚠️ 教训：**跨模块调用的函数，名字要对得上；
加完诊断后要亲眼确认日志出现了**，不能假设它会输出。

#### 新增诊断日志（供真机排查）

本次在画框链路埋了 6 个断点，每一条都能直接定位
「事件到没到」与「判据对不对」：

| 日志 | 含义 |
|---|---|
| `anno:bind` | 每页绑定成功，带实际的 `ta` / `pe` 值 |
| `anno:down` | pointerdown 到了，带 `hit=box/none` |
| `anno:drag` | 位移超阀，确认拖拽 |
| `anno:up` | pointerup 到了，带 `pending` / `dragging` |
| `anno:delete-try` | 走删除分支，带 `onBox` / `hitBox` |
| `anno:reject` | 框太小被丢弃（带实际 w/h） |
| `anno:added` | 框已存入 regionMarks |

**打开方式：设置 → 通用 → Logger 打开 → 浮层里展开 → Copy。**

判读：

- 没有 `anno:bind` → 选项/挂载时机有问题
- 有 `anno:bind` 但按下去没 `anno:down` → **事件被 WebView 吞了**
- 有 `anno:down` 但没 `anno:added` → 看 `anno:reject` 的 w/h

本地实测（日志已确认输出）：

```
anno:bind | page=1 ta=manipulation pe=auto
anno:down | hit=box page=1 pe=auto ta=manipulation
anno:up   | cancelled=false pending=true dragging=false
anno:delete-try | onBox=true hitBox=true
```

功能：画框 1、点框 0 ✓

---

## [0.1.25] - 2026-09-25

### 「框依旧无法删除」—— 第二次才找对尺子 + 简称页返回键

#### 1. 框删不掉：用错了度量

0.1.24 我把删除改到 `pointerup` 里做命中测试（不再依赖 click），
但还是不行。

❌ 上一版的错：**用「位移」这一把尺子去量两件不同的事**。

```
位移 < 阈值  →  点击（可能删除）
位移 > 阈值  →  拖拽（画框）
起点在框上时把阈值放宽到 2 倍
```

真机「点一下」时手指压在屏幕上本身就有微动（加上滚动惯性），
`pointermove` 序列的累积位移轻松超过 2 倍阈值（≈22px）——
于是被判成"拖拽"，走画框分支，画出一个与原框重合的新框，
看上去就是"点了没反应，框还在"。

✅ 正解：**按下的位置本身就已经表达了意图**。

| 按在哪 | 意图 |
|---|---|
| 已有框上 | 处理这个框（删除）。**位移多少都不画框** |
| 空白处 | 可能是画新框 |

在 `pointerdown` 里定下 `onBox`，`pointermove` 一旦看到
`onBox` 就**直接 return**（连鬼框都不建 —— 建了会闪一下虚线，
让用户以为要画框了）。两个意图彻底不重叠。

顺带修一处：删完框要把随后的补发 click 吃掉。
因为框已不在 DOM，`closest('.anno-box')` 失效，
`event.target` 变成底下的 layer，那一次点击会顺带切菜单
（“删个框菜单乱跳”）。

#### 2. 简称页没接系统返回键 → 一返回直接退出 App

用户：「返回页你怎么又犯了不允许原生返回的操作？
一返回直接退出 app 了」。

根因：Scholarius 是单页应用，`WebView.canGoBack()` 永远是 false。
只要 `handleBack()` 走到最后 `return false`，原生就退出应用 ——
不管屏幕上正开着一个多明显的全屏覆盖层。
0.1.23 新做的简称页忘了加进去。

✅ 修法：在 `handleBack()` 里加一条（排在 `isSheetOpen()` 之后 ——
因为简称页里还能弹「编辑表单」，那也是 `.sheet`，
应优先关表单）。

⚠️⚠️ 并新增 **`tools/check_back_layers.py`** 防再犯：
自动枚举 `index.html` 里所有全屏覆盖层
（`role="dialog"` + `aria-modal="true"` 的 `<section>`），
逐个比对 `handleBack()` 是否认领。
本机无 JDK 也能跑。
（该脚本已自测：把认领临时抹掉，确实能报出来。）

#### 实测

框：

| 操作 | 结果 |
|---|---|
| 空白处拖拽 | 画框 ✓ |
| 点框中心 | 删除 ✓ |
| **按在框上拖 40px** | **只删除，不画重合框** ✓ |
| 按在框上抖 5px | 删除 ✓ |
| 竖直拖拽（细框） | 画框 ✓ |
| 点空白 | 框不变、菜单切换 ✓ |
| 保存重进 | 框持久化 ✓ |

返回键：

| 状态 | `handleBack()` |
|---|---|
| 简称页开着 | `true`（不退出）✓ |
| 编辑表单开着 | `true`（不退出）✓ |
| 都关掉 | `false`（允许退出）✓ |

---

## [0.1.24] - 2026-09-25

### 三个真机才能复现的 bug（点击生框 / 框删不掉 / 简称改了卡片不刷新）

三个都是「桌面能跑、手机不行」或「只有真机才能看到」的类型，
Playwright 默认跑不出来 —— 这次是靠用户反馈定位的。

#### 1. 「我没拖拽，点了一下直接生成了一个框」

根因：`DRAG_SLOP = 8` 是写死的 **CSS 像素**，太小。
真机手指"点一下"的天然抖动轻易就有 10~15 CSS px（高 DPI 屏更明显），
于是每一次轻点都被判成"拖拽"。

修法：

- 阈值改用**物理尺寸**定义：`3mm`，按 CSS 基准密度换算（≈ 11.3px）。
  比 Android 的 `getScaledTouchSlop()`（≈1.4mm）宽一倍多，
  又远小于"想画一个框"的必要位移（> 5mm）。
- 位移量改用**欧氏距离**而不是 `max(|dx|,|dy|)` ——
  后者会把"两方向各移 8px"（实际 11.3px）当作没动。
- 新增一条：**起点落在已有框上且没走远时，不当作画新框**。
  用户想删框时手指抖一下，不应该在框正上方生出一个重合的新框。

#### 2. 「框无法删除」

根因在 CSS，很隐蔽：

```css
.reader.is-annotating .anno-layer.is-drawing {
    touch-action: none;    /* ← 必须的，但它把 click 掐了 */
}
```

`touch-action: none` 是**必须**的（不给的话手指拖拽被浏览器解释成
滚动页面，`pointermove` 收不到几个点，框只画出一小段）。
但它在真机上让整块**不再派发 click** —— 浏览器认为这里是"拖拽区"，
不会补发 click。于是 `.anno-box` 上那个 click 监听形同虚设：
**鼠标调试一切正常，真机上永远不触发**。

✅ 修法：删除改在 `pointerup` 里用坐标自己做命中测试
（新增 `boxAt()` + `pending.hitBox`），与"画新框"共用同一套
pointer 事件，**不再依赖 click**。

并删掉 `makeRegionBox` 里那个失效的 click 监听 ——
留着会变成"删两次"（真机不触发所以看不出，鼠标调试一次删两个框）。

#### 3. 「修改了简称，文库页卡片依然是旧简称」

卡片上的载体名是 `venueNameOf(doc)` 查 `ScholariusShortcut.lookup()` 得来的，
但**卡片不知道自己依赖这张表** —— 它的渲染早就完成了。
简称页改完只刷新了自己的列表，没人叫文库页重绘。

✅ 修法：暴露 `ScholariusVault.render()`，
简称页在**新增 / 修改 / 删除**三处都调 `notifyVault()`。

删除也要通知 —— 删掉之后卡片要退回显示全名。

⚠️ 没有做成"每次渲染都重读 localStorage"：卡片渲染是高频路径
（搜索每敲一个字都走一次），改成**事件驱动**更合适。

#### 真实流程实测（非模拟）

| 场景 | 结果 |
|---|---|
| 拖拽画框 | 0 → 1 ✓ |
| 点框 | 1 → 0（删掉了）✓ |
| 画两个框 | 0 → 2 ✓ |
| 点第一个框 | 2 → 1（精确删除）✓ |
| 点空白 | 框不变、菜单切换 ✓ |
| 点击抖动 9px | 不画框 ✓ |
| 简称 NIPS → NeurIPS | 卡片标题立即跟着变 ✓ |
| 删除简称 | 卡片退回全名 ✓ |

---

## [0.1.23] - 2026-09-25

### 手势冲突真正修好 + 发表物简称可改可删 + 三个图标隐形事故

#### 1. 「原始视图无法隐藏菜单」—— 第四次才修对

用户从 0.1.19 起连续反馈三次，0.1.22 我自作主张
「原始视图整段跳过这个手势」，结果更糟：

> 「依旧无法隐藏菜单，这代表依旧无法选中顶部底部文字」

因为菜单本来就是收起的，跳过之后**永远唤不出来**。

前四次的修法及错误：

| 次数 | 做法 | 为什么错 |
|---|---|---|
| ① | 只在阅读视图判中间 1/3 | 只是**缩小**冲突范围 |
| ② | 编辑模式时跳过 | 同上 |
| ③ | 限定中间 1/3×1/3 | 同上 |
| ④ | 原始视图整段跳过 | 菜单**永远唤不出** |

✅ 最终：**用位移区分点击与拖拽**（手势的天然语义）。

- `pointerdown` 只记起点，**不**立刻开始画
- `pointermove` 位移超过 `DRAG_SLOP`(8px) 才认定为拖拽并开始画
- `pointerup` 未超阈值 = 普通点击 → 不画框，click 正常冒泡去切菜单

两者互斥，不可能同时成立。实测：原始视图里点正中/顶部 10%/底部 92%
四个位置菜单都能来回切，且**没有一次误画框**。

#### 2. 画完框菜单会跳一下

`pointerup` 后浏览器会**补发一个 click**，而它正好命中
「点屏幕切菜单」的监听器。新增 `markDragged()` / `justDragged()`
吃掉它 —— 与 `justLongPressed` 一样带自动过期兜底。

#### 3. 发表物简称：无法删除 + 无法修改

用户反馈「设置里面的 venue 简称，无法删除和修改」。两个问题：

- **无法修改** —— 根本没有编辑功能，只能删掉重建，
  而「全名」很长（"Neural Information Processing Systems" 38 字符），重打一遍极烦
- **无法删除** —— 删除只能点中右下角那个 40x40 的小图标，
  手机上手指稍一移动就被 WebView 判定成滚动，click 不触发

✅ 改为：**点整行 → 弹编辑表单**（整行 467x61，比 40x40 大得多）

- 底部：取消 / 保存
- 右上：删除（红）—— 不放在底部，因为底部右边必须留给主操作

#### 4. 编辑表单标题不居中

用户指出。原写法 `justify-content: space-between` 只有
「标题」和「删除」两个孩子，标题被顶到最左边。
用 `::before` 补一个与删除等宽的占位，标题才真居中
（实测偏差 0px）。

#### 5. 「添加」改成加号图标

用户要求「用个加号图标就行了，不需要用文字」。
省下的横向空间留给简称输入框。

#### 6. 简称页改用返回键，不用叉号

用户指出「这不是独立弹窗，而是设置项的子页」，
所以头部改成 **左上返回 + 居中标题 + 右占位**
（与账户详情页同一套）。叉号留在目录弹窗那里 —— 它确实是浮层。

#### 7. ⚠️ 两个新图标一个像素都不渲染

往 `ICON_PATHS` 加 `add` / `trash` 时**漏了 `ICON_VIEWBOX` 登记**，
viewBox 落到默认 `0 0 24 24`，而路径坐标是 960 体系的
（y ∈ [-960, 0]）→ 图形整体在视口外。

症状极具欺骗性：元素尺寸正常、`getComputedStyle` 全对、不报错，
但 canvas 数非透明像素得到 **inkPixels = 0**。

新增 `tools/check_icon_viewbox.py` 防这一类漏登记
（逐张表交叉比对，本机无 JDK 也能跑）。

---

## [0.1.22] - 2026-09-25

### 修「框画不出来」+ 原视图不再被手势打断 + 标签移位

#### 1. 误触判据连错两版，修好了

用户实测「拖了半天什么都没出现」。根因在**误触阈值**上，
我改错两次，两版都记在注释里：

| 版本 | 判据 | 后果 |
|---|---|---|
| ① 原始 | `w < MIN \|\| h < MIN`（两轴各自达标） | 竖直拖拽 w≈0 → 丢弃 |
| ② 中间 | `w * h < MIN²`（面积） | 纯竖直时 w **恰好为 0**，面积恒 0 → 仍丢弃 |
| ③ 现在 | `max(w, h) < MIN`（至少一方向够长） | ✓ |

③ 才对得上「误触」的真实定义：误触 = 点一下**两个方向都小**；
竖直框 w≈0 但 h 很大，必须保留。

#### 2. 退化边给最小宽度，否则存了也白存

`AnnotationStore.load()` 会把 `x1 <= x0` 的框当退化数据**丢掉**。
而竖直拖拽的宽就是 0 —— 用户当下看得见框，重进页面后
**凭空消失**。所以给两条边都擑到至少 `MIN_EDGE`(0.002)。

实测：竖直拖拽得到宽 0.2% 的细框，存下后重进**仍在**。

#### 3. 原始视图不再用手势切菜单

用户第三次指出，这次把根因说清了：

> 「单击屏幕是隐藏菜单，画框从屏幕一个地方到另一个地方——
>   不隐藏菜单，顶部底部都没法选中」

这是个**设计冲突**：单击 = 切菜单，拖拽画框 = 不该切菜单。
前两次修（编辑模式跳过、限定中间 1/3×1/3）都只是在**缩小冲突**，
没消除 —— 只要 `bodyEl` 还监听 click，画框结束的那个 click 就会命中它。

现在**用手势语义而非坐标区分**：原始视图是「操作版面」场景，
在那里一次点击**永远不该**被解释成「藏菜单」。整个视图不接这个手势。

实测：顶部→底部拖拽，菜单 `false → false` 不再被切换。

#### 4. 框上标签移位（用户要求）

「删除按钮布局向上一些，向右一点」——
标签从框左上角**内侧**（`top:0; left:0`）改到**外侧偏上偏右**
（`top:-14px; left:6px`）。

内侧会压住框里的内容（而框住的东西往往正好在左上角），
且与「从此处起笔画新框」的位置重叠。实测新位置相对框左上角
偏移 `Δx=+7, Δy=-13`。

---

## [0.1.21] - 2026-09-25

### 清除改悬浮按钮 + 修「没东西可清」

#### 1. 「清空」改成悬浮按钮（FAB）

用户：「清除用悬浮按钮，跟着唤醒菜单一起唤醒和隐藏」。

原来把它当底栏第五项，问题有二：底栏四项是**互斥的开关组**
（模式），清空是**一次性动作**，混在一起语义不齐；且底栏会随菜单
一起滑走，而用户往往正是在翻看内容时发现「一团乱麻」。

现在 FAB 贴左下、圆底 + `--danger` 色（**不用阴影**），
与菜单同一条 transition 曲线一起出现/隐藏。底栏回到干净的四项。

#### 2. 修「点击清除提示没有可以删除的，但框都实实在在地在那里」

这个 bug 有**两层**原因，都修了：

**① 命名空间搞混了（主因）**

我原来写 `var scope = annotateMode`，但：

| | 取值 |
|---|---|
| `annotateMode`（编辑栏四项） | `text` / `formula` / `table` / `figure` |
| 清空要比对的（文本类型） | `title` / `author` / `abstract` / `body` / `heading` / … |

两者**毫无交集**。所以 `scope='text'` 时去找「类型是 text 的块」，
一个都没有 → 提示「没有可清空的」，而屏幕上明明有 15 个`章节标题`框。

现在清空**默认作用于全部文本类型**。

**② 「框」与「已标标注」是两回事（用户点出的关键）**

框架里有**两层**类型来源：

- 原生自动识别（`block.kind`）→ 用户**看到**的那些框
- 用户标注（`textMarks`）→ 用户**改过**的

旧实现只数第二层。一篇刚导入、还没标过的论文，第二层是空的，
所以提示「没有可清空的」——但屏幕上明明写着 15 个 `章节标题`。
**用户看到的是框，框就是识别结果本身。**

现在清空作用于**有效类型**（标注优先、否则原生）：命中类型的块
被显式标成「正文」，用标注去**覆盖**原生判断。语义上也说得通——
「我不同意你判的，我按我的来」，而且可保存、可再改。

（不能真去改 `block.kind`：那是提取层产物，重进页面就重算，留不下来。）

**③ 顺带修：确认数字夸大十倍**

只数「有效类型等于 scope」会把**本来就是正文**的块也算进去。
实测 ResNet 全篇 1110 块里只有 107 个是原生判的 heading，
却显示「将清除 1110 个区域」——数字是**假信息**，用户会被吓到。
现在只数「消除后类型会变」的块，显示 **107**，与实际动作完全一致。

**④ 顺带统一两处样式路径**

`refreshTextBlockStyles` 用 `markAtLine`、`makeTextBlockEl` 用
`nativeMark`，两套步调不一致 —— 症状是「清空后重进又变回标题」。
现在都走 `effectiveMarkAt`。

#### 验证（ResNet 实测）

| | 修复前 | 修复后 |
|---|---|---|
| 点清空 | 提示「没有可清空的」 | 弹确认：将清除 **107** 个区域 |
| 确认后 | — | 第1页 15 个 heading 框 → 全 body |
| 保存 | — | 107 条，全 body，覆盖 107 块 |
| 阅读视图 | 85 个碎片区域 | **2 个**（标题 + 作者） |

---

## [0.1.20] - 2026-09-25

### 修正编辑模式的实际可用性（用户五条反馈）

上一版我说"验收通过"，其实是**自欺欺人** —— 我测的是"脚本能不能改对"，
不是"**人**能不能改得动"。用户回：「我这里改起来简直困难重重」。
五条一并修掉。

#### 1. 进编辑模式就显示所有框（不再需要点选项）

原来必须点某个选项才显示对应的框，因果搞反了：用户点选项是**表达意图**，
不是"请求显示"。而且进来只看到一张干净页图，根本不知道哪里判错了。

现在文字块**始终**挂上（含三个矩形模式），进编辑模式即见现状。

#### 2. 选中态改成**真正的填充图标**

原来注释里写着"用颜色强调 + 字重近似" —— 假的就是假的，用户看得出来，
并且指出了两次。现在改用**两只真 SVG**（outline / fill），
与底部导航栏 `.nav-item` 逐字一致：

- 两个 `svg` 用 `grid-area: 1/1` 叠同格，按 `aria-pressed` 切 `display`
- Material Symbols 的 outlined / filled 是**两套不同路径**，
  实测 12 个标注图标里 **6 个确实不同**（table_chart / image / group /
  article / sticky_note_2 / menu_book / sell）
- 新增 `ICON_PATHS_FILL` 与 `iconFilled()`，路径全部取自官方 CDN

实测（`表格` 选中时）：

| 项 | outline | fill | 颜色 | 阴影 | 背景 |
|---|---|---|---|---|---|
| 未选中 | block | none | `#7E7E7C` | 无 | 透明 |
| **选中** | **none** | **block** | **`#F2F2F0`** | **无** | **透明** |

阅读视图选项与类型选择弹层同样处理。

#### 3. 新增「清空」（按类型整体清除）

原生自动识别在真论文上错得离谱：ResNet 107/1110 块被判成 heading，
Transformer 几乎整篇都是 heading。这种量级下逐块改不可能完成。

底部编辑栏新增第五项「清空」，走确认表单，并说清影响几个框：

> 清空这些框？
> 将清除 1 个「全部类型」区域。PDF 本身不受影响。
> 　　　　　　　　[取消] [清空]

确认后才执行。未选任何类型时清空**全部**。

#### 4. 修「编辑时点框会收起菜单」这个大 bug

根因有两处，都在 `mountTapToToggle`：

- **编辑模式下不跳过**：点框会命中切换菜单的逻辑，菜单忽隐忽现，
  框也点不干净。现在编辑模式整段跳过。
- **只判横向不判纵向**：导致靠近顶部/底部的区域（横向中间）
  也会切换菜单，把那一小块盖住 —— 这就是用户说的
  「pdf 的顶部和底部变得无法编辑」。现在限定在中间 1/3 × 1/3。

#### 5. 表单文案与按钮

- 标题 `改为哪一类` → **`编辑`**（点已有框想的是"改这个"，不是"新建"）
- 底部按钮 → **`[取消] [删除]`**（删除＝去掉该块标注，不改 PDF）
- 「选择一段区间」按钮**整个删掉**。

  第一次我只删了它上面的说明文字，用户随即追问
  「选择一段区间这个提示不是没删吗？」—— 对，要删的是按钮本身。
  它是多余的中间步骤（把两步变四步），还常驻弹层里。

  区间能力改由**长按**触发（长按某块设起点，再点终点即覆盖整段），
  不占界面、不打断「点框 → 选类型」两步主流程。

  附带修了一个真 bug：`justLongPressed` 的标记原本只靠"读一次清零"，
  而浏览器长按后**不一定**补发 click，标记会一直挂着把用户
  下一次真实点击吞掉（实测：长按设起点后点终点毫无反应）。
  现在加了 1200ms 自动过期兜底。

---

## [0.1.19] - 2026-09-24

### 让「用编辑模式把阅读视图改对」真正可行

验收标准是「能不能靠编辑模式把一篇论文的阅读视图完全编辑对」。
实测**做不到** —— 不是缺交互，是底下三处硬伤让工具根本够不着。
这版把三处都修了，并在两篇真论文（ResNet / Transformer）上验证通过。

#### 1. 六个作者挤成一个盒子（`tools/make_dev_library.py`）

`Kaiming He` / `Xiangyu Zhang` / `Shaoqing Ren` / `Jian Sun` /
`Microsoft Research` / `{kahe,...}@microsoft` 六行在 pymupdf 里是
**同一个 block**，所以六行拿到**像素级完全相同**的框
（实测都是 `108,150 255x34`）。`elementFromPoint` 永远命中最后一个 ——
用户看到一行、点下去改的是另一行。

改为**行级优先**索引（行级盒每行独立）：

| 行 | x | 宽度 |
|---|---|---|
| Kaiming He | 22.29% | 9.6% |
| Xiangyu Zhang | 36.28% | 12.21% |
| Shaoqing Ren | 52.89% | 11.01% |
| Jian Sun | 68.3% | 6.67% |

全篇塌陷 **657 → 198**（ResNet），唯一盒 183 → **642**。

#### 2. 大盒盖住小盒，76 个块里 30 个点不到

段落盒（跨多行）会盖住它内部的每个行盒，因为段落盒**后挂** ——
DOM 里后挂的在上层。改为**按面积降序挂**（大盒先入、小盒在上），
视口内命中率 **60% → 87%**。

同属项目里反复出的「元素存在、display 正常、却点不到」那一类，
判据永远是 `elementFromPoint`，不是"元素在不在"。

#### 3. 章节标题**无法指定层级** → 补上

`applyTextType` 写死 `level: block.level || 1`，而原生的 level
要么是 0（判不出）要么猜错，于是：用户标的所有标题都变成 level 1，
阅读视图永远是**平坦**的一堆 lv1，用户要求的
「每个一级章节区域可以再分」**根本无法表达**。

现在选「章节标题」会**多一步问层级**（一级/二级/三级），
层级由用户显式指定、不再从原生借。`openRegion` 的嵌套栈本就是现成的，
只差输入通道。实测已能产出真嵌套：

```
rd-region-lv1 「3. Deep Residual Learning」
  └ rd-region-lv2 「3.1. Residual Learning」
```

#### 4. 补**区间选择**（否则摘要根本标不动）

原生的 heading 判据把 ResNet 的摘要切成 **14 个交错的
heading/paragraph 块**（块 7~20）。逐块点的话用户要点 14 次、
每次只覆盖一行 —— "把整段摘要标成摘要"这件事做不到。

做法：点起点 → 「选择一段区间」→ 点终点 → 选类型。
**14 次点击降为 2 次**，摘要一次标完。

#### 验证结果（两篇真论文，实测）

| | 修复前 | 修复后 |
|---|---|---|
| ResNet 作者 | 不存在（6 个 lv1 章节） | 1 个完整作者区 |
| ResNet 摘要 | 不存在（14 个碎片） | 1 个完整摘要区 |
| Transformer | 几乎每块都误判成章节标题 | 标题/作者/摘要 各 1，章节 7 |

### 其它

- `AnnotationStore` 的 level 上限从 6 改为 **3**，与前端
  `MAX_HEADING_LEVEL` 一致 —— 否则会出现"存得下但渲染不出差别"的数据。
- 块小标签在章节标题后带层级（`章节标题 L2`），层级也写进
  `anno-block-heading-lv2` 类，用**色条粗细**区分（缩进会和 PDF
  绝对坐标错位，所以不能用缩进）。
- 修掉 `refreshTextBlockStyles` 整串覆盖 `className` 的隐患。

---

## [0.1.18] - 2026-09-24

### 让「用编辑模式把阅读视图改对」真正可行

验收标准是「能不能靠编辑模式把一篇论文的阅读视图完全编辑对」。
实测**做不到** —— 不是缺交互，是底下三处硬伤让工具根本够不着。
这版把三处都修了，并在两篇真论文（ResNet / Transformer）上验证通过。

#### 1. 六个作者挤成一个盒子（`tools/make_dev_library.py`）

`Kaiming He` / `Xiangyu Zhang` / `Shaoqing Ren` / `Jian Sun` /
`Microsoft Research` / `{kahe,...}@microsoft` 六行在 pymupdf 里是
**同一个 block**，所以六行拿到**像素级完全相同**的框
（实测都是 `108,150 255x34`）。`elementFromPoint` 永远命中最后一个 ——
用户看到一行、点下去改的是另一行。

改为**行级优先**索引（行级盒每行独立）：

| 行 | x | 宽度 |
|---|---|---|
| Kaiming He | 22.29% | 9.6% |
| Xiangyu Zhang | 36.28% | 12.21% |
| Shaoqing Ren | 52.89% | 11.01% |
| Jian Sun | 68.3% | 6.67% |

全篇塌陷 **657 → 198**（ResNet），唯一盒 183 → **642**。

#### 2. 大盒盖住小盒，76 个块里 30 个点不到

段落盒（跨多行）会盖住它内部的每个行盒，因为段落盒**后挂** ——
DOM 里后挂的在上层。改为**按面积降序挂**（大盒先入、小盒在上），
视口内命中率 **60% → 87%**。

同属项目里反复出的「元素存在、display 正常、却点不到」那一类，
判据永远是 `elementFromPoint`，不是"元素在不在"。

#### 3. 章节标题**无法指定层级** → 补上

`applyTextType` 写死 `level: block.level || 1`，而原生的 level
要么是 0（判不出）要么猜错，于是：用户标的所有标题都变成 level 1，
阅读视图永远是**平坦**的一堆 lv1，用户要求的
「每个一级章节区域可以再分」**根本无法表达**。

现在选「章节标题」会**多一步问层级**（一级/二级/三级），
层级由用户显式指定、不再从原生借。`openRegion` 的嵌套栈本就是现成的，
只差输入通道。实测已能产出真嵌套：

```
rd-region-lv1 「3. Deep Residual Learning」
  └ rd-region-lv2 「3.1. Residual Learning」
```

#### 4. 补**区间选择**（否则摘要根本标不动）

原生的 heading 判据把 ResNet 的摘要切成 **14 个交错的
heading/paragraph 块**（块 7~20）。逐块点的话用户要点 14 次、
每次只覆盖一行 —— "把整段摘要标成摘要"这件事做不到。

做法：点起点 → 「选择一段区间」→ 点终点 → 选类型。
**14 次点击降为 2 次**，摘要一次标完。

#### 验证结果（两篇真论文，实测）

| | 修复前 | 修复后 |
|---|---|---|
| ResNet 作者 | 不存在（6 个 lv1 章节） | 1 个完整作者区 |
| ResNet 摘要 | 不存在（14 个碎片） | 1 个完整摘要区 |
| Transformer | 几乎每块都误判成章节标题 | 标题/作者/摘要 各 1，章节 7 |

### 其它

- `AnnotationStore` 的 level 上限从 6 改为 **3**，与前端
  `MAX_HEADING_LEVEL` 一致 —— 否则会出现"存得下但渲染不出差别"的数据。
- 块小标签在章节标题后带层级（`章节标题 L2`），层级也写进
  `anno-block-heading-lv2` 类，用**色条粗细**区分（缩进会和 PDF
  绝对坐标错位，所以不能用缩进）。
- 修掉 `refreshTextBlockStyles` 整串覆盖 `className` 的隐患。

---

## [0.1.18] - 2026-09-24

### 按反馈修正标注交互

v0.1.17 的标注功能方向对，但交互位置与默认状态都不对，四条一并改掉。

#### 1. 阅读视图**去掉折叠**

阅读视图是**结果**，不是编辑界面 —— 它显示的是"我们判定的分区"，
用户在这里唯一该做的是**读**。要改分区就去原始视图
（那里能看到版面、能画框、能选中文字）。

所以上一版加的可点标题、折叠三角、`aria-expanded` 全部删掉，
标题回归纯 `<h2>/<h3>/<h4>`。留一个可点的标题会让用户以为
"点一下能改什么"，而点了没反应比没有这个交互更让人困惑。

#### 2. 编辑按钮改为**切图标**，去掉点击特效

两个图标（`edit` / `edit_off`），默认 **`edit_off`**，点击进入编辑模式变 `edit`。
与「阅读/原始」视图切换按钮同一套做法：两只图标一次性预置，
切换只改 `hidden`，不重建 `innerHTML`（重建会让图标闪一下）。

#### 3. 编辑选项改为**四类**：文本 / 公式 / 表格 / 图片

加了「文本」。前三个是手画矩形，**「文本」是另一种操作**：
点一下页面上的文字区域即可重新归类。

#### 4. 编辑选项移到**下方选项栏**

原来贴在顶栏下方是错的。用户指出：

> 下面的选项栏都是为阅读视图准备的，切换到原始视图就不需要了，
> 所以下面的选项栏直接换成编辑的这四种选项就可以了

理由很实在：底部那条栏是**视图专属**的。「目录 / 设置」对原始视图
没有意义（没有重排正文，也就没有目录可跳、没有字号可调）。
与其让它们留在那里点了没反应，不如整条换掉。

选中态与底部导航栏**同一套**：强调色 + 加粗（不用背景块那种"chip"式）。

#### 关于「文本」模式：为什么用可点的文字区域

这里有个绕不过去的事实：**原始视图里的页是位图**，
位图里没有可选的文字，`document.getSelection()` 在页面上永远返回空。
所以"在 PDF 上拖选文字"物理上做不到。

但需求是可实现的，只要换个手法：`PdfText` 已经算出了每个块在页面上的
包围盒（`Block.x0/y0/x1/y1`，v0.1.17 加的坐标管道），把它叠在页图上
就是一个**可点的"文字区域"**，点一下即选中，再弹出八类选择。

这仍然符合「框只能是方的，但区域可以根据文本来」——
这里的框**不是用户拖的**，是从文字自身范围算出来的，所以它贴合文字。

#### 修掉一个隐蔽的 bug

`.reader-action` 写了 `display: flex`，而 **`hidden` 属性只对应
UA 样式表的 `display: none`，任何作者样式都会覆盖它** ——
于是底栏里「目录 / 设置」虽然 `hidden="true"` 却依然显示，
编辑模式下底栏出现**六个**选项挤成两排（截图才发现）。

这个 bug 的隐蔽处：`element.hidden` 读到的是 `true`，看起来"已经隐藏了"。
**判断"藏没藏"要看 `getComputedStyle().display`，不是看属性。**

顺带加了 `tools/check_i18n_keys.py`（查双语键对称）——
第一次跑就抓到一个真缺失的键：改文案时两行被粘成一行，
语法仍合法所以 `node --check` 查不出来，症状是某种语言下显示原始键名。

---

## [0.1.17] - 2026-09-24

### 新增：自己分不对？那就让你来分 —— 用户标注 + 文本分级

用户提议：「我们不自己分了，我们留给用户分……让用户根据 pdf 自己划分？
比如先是文本，再是公式，然后表格，再是图片」，
随后给出了具体的交互与一个关键洞察。

#### 关键洞察：文本与非文本是**两套机制**，不能混着做

> 「标题、摘要、作者肯定都是文本啊，可以选中；我说的不是框，
>  而是类似选中的区域，框只能是方的，但区域可以根据文本来」

|  | 文本区域 | 非文本区域 |
|---|---|---|
| 内容 | 标题 / 作者 / 摘要 / 正文 / 章节标题 / 脚注 / 参考文献 / 关键词 | 公式 / 表格 / 图片 |
| 边界 | 由**文字自身**决定（哪一行到哪一行） | 手画**矩形** |
| 操作 | **选中文字** → 指定类型 | 拖矩形 → 指定类型 |

给文本加方框是错的 —— 标题居中、摘要两端对齐、正文有缩进，
方框会框进旁边的栏、页眉、公式编号，用户看到的框是错的。

#### 一、文本分级（阅读视图）

正文现在按**结构**切成区域，不再是平铺的一串：

```
标题区  ── 论文题目
作者区  ── 作者 + 单位（位置判据：紧跟题目之后）
摘要区  ── Abstract 标题 + 摘要正文
1 Introduction            ← 一级章节区
3 Deep Residual Learning  ← 一级章节区
  └ 3.1 Residual Learning    ← 二级（可折叠）
  └ 3.2 Identity Mapping     ← 二级（可折叠）
```

- **分到哪一层由用户决定**：每个章节标题都可点折叠/展开，
  收起一级会把其下所有层级一起收起。默认全展开。
- 首页区（标题/作者/摘要）带小字标签说明"这段是摘要"，
  因为它们是自动识别出来的分区，不标注用户会以为是排版巧合。
- 折叠**只改可见性、不重建 DOM** —— 重建会把滚动位置弹回顶部。

#### 二、PDF 视图的标注编辑（用户手画矩形）

- 顶栏新增**铅笔按钮**（只在原始视图出现）。点它进入标注模式。
- 顶部出现类型条：**公式 / 表格 / 图片**，点选后在页面上拖拽画框。
- 颜色按类型区分（公式蓝 / 表格绿 / 图片紫），框上带类型标签。
- **点已有框即删除**（不弹确认 —— 编辑时最常见的意图是"刚才画错了"，
  弹窗会打断连续修正的节奏）。
- 太小（小于页宽 1.2%）的拖拽视为误触，不生成框。
- 退出标注模式或关闭阅读页时**自动保存**。

#### 三、用户标注覆盖自动判断

标过的区域以标注为准，没标过的仍用自动结果。

#### 实现要点（都是踩过的坑）

- **浮层必须与图片实际显示区域严格重合**。页图用
  `object-fit: contain`，长宽比不符时会有留白；浮层若直接铺满容器，
  存的归一化坐标会偏移"不多不少刚好是留白宽度"，极难发现。
  所以浮层尺寸由 JS 按 `naturalWidth/naturalHeight` 算，
  并在图片 `load` 后重算一次。
- **画框用 Pointer Events**（不用 mouse + touch 两套），
  且浮层必须写 `touch-action: none` —— 不写则手指拖拽被解释成滚动页面，
  画出来的框只有一小段。
- 坐标用**百分比**定位，转屏/分屏时框自动跟着对，不需要监听 resize。
- 标注存 `library/<docId>/annotations.json`，与 PDF 同目录
  （删文献时一起删干净）。写入走"临时文件 + rename"，
  避免写到一半被杀进程留下截断的 JSON。
- 拖拽落盘只在**退出编辑模式 / 关闭阅读页**时做，不是每画一个框都写。

#### 顺带修的

- `Blocks` 现在带归一化包围盒（`x0/y0/x1/y1`），
  `Result` 带每页宽高比（含 `/Rotate` 90/270 的宽高对调）。
- 新增 `tools/check_kotlin_structure.py`：本地无 JDK，
  推 CI 前先自查括号闭合与重复局部声明（0.1 秒，替代 3 分钟的 CI 轮回）。

---

## [0.1.16] - 2026-09-24

### 修复：正文抽取「一团乱麻」——分段阈值按错了排版

用户反馈：「能不能用 python 抽取 pdf 内容啊？现在的抽取结果太混乱了」，
并补充「就一团乱麻」，涉及段落拆碎、标题认不出、公式乱码、
页眉页码混入、换行错误五项。

**先说结论：不是引擎问题，是规则写错了。** 所以没有换语言。

#### 为什么不换 Python

- **Python 跑不到 Android 上**。要么用 Chaquopy 把 CPython 打进 APK
  （体积 +20~40MB，现在是 14.7MB），要么把 PDF 传到服务器解析 ——
  后者直接违反项目已定的「服务端绝不存 PDF / 文献全文」约束。
- 而且 **PDFBox 与 PyMuPDF / pdfplumber 是同一代技术**，
  能拿到的信息完全一样。实测确认：字体名、字号、坐标**全都有，质量很好**。

#### 真正的根因（有实测数据）

`PdfText.looksLikeHead()` 用 `长度 <= SHORT_LINE(60)` 判断"是否独立块"。
**那个 60 是按双栏宽行（每行 60-90 字符）拍的。** 而用户论文库里
大量**单栏窄行**排版，中位行宽只有 10-36 字符 —— 于是：

| 论文 | 行数 | 中位行宽 | 被误判成独立块 |
|---|---|---|---|
| 2015 LeCun | 506 | 21 | **54.0%** |
| 2016 ResNet | 949 | 20 | **71.2%** |
| 2017 Transformer | 401 | 10 | **72.1%** |
| 2022 CoT | 627 | 36 | **57.6%** |

**判据：绝对字符数阈值不能跨排版使用。** 行宽差 3-5 倍时，
任何固定值都只能对一半文档有效。

同时还有一个更严重的实现错误：**字号与粗体这两个最强的信号
在 `classifyBlock` 里算出来了，却没有参与分段决策**。
实测 ResNet 的 `1. Introduction`（12pt Bold）被判成普通行，
而一堆正文行（10pt Regular）反被判成独立块 —— 正好反了。

#### 改法

以**字号/粗体（相对判据）**主导分段：

1. **分段改用相对判据**：`字号 > 正文字号 + 0.35pt` 或粗体 → 独立块。
   字符数不再是主判据（`SHORT_LINE` 常量已删除，并在原处留注释说明原因）。
2. **正文字号估计改为「页级众数再投票」**。
   直接取全局众数会被封面页带偏 —— 实测 HAL 版 LeCun 论文
   封面 10.9pt、正文 9.3pt，直接取众数得出 6.8pt（全错），
   于是正文全被判成"比基准大 → 标题"。改成每页先投一票后
   封面只占一票，结果正确（9.3pt）。
3. **丢弃字号远小于正文的行**（`< 正文 × 0.75`）。
   ResNet 有一整批 3.3pt 的 `0 1 2 3 4 5 6`（训练曲线的坐标轴刻度）
   和 6.6pt 的 `training error (%)`。它们不是句子、数量极大，
   会把段落统计彻底污染。
4. **连字还原**：`ﬁ` `ﬂ` → `fi` `fl`（实测四篇共 64 处）。
   不还原的话复制出去是乱码，且搜 `difficult` 搜不到 `difficult`。
5. **断词还原**：`learn- ing` → `learning`（实测 64 处）。
   正则在**合并后**的文本上匹配（`(\w)- (\w)`），
   因为合并时是用空格连接的 —— 按原文换行匹配不会命中。
6. **章节号单独成行时与标题合并**。
   PDF 有两种排法：`3.1 Problem` 与 `3.1` + 下一行 `Problem`。
   后者若按"短行 → 独立块"处理会得到一堆 3 字符碎片
   （实测 Word2Vec 段落中位数从 400+ 掉到 13）。
7. **表格数据行不当标题**。ResNet 的 82 个"标题"里有 31 个
   其实是表格行（`28.54 25.03 ResNet-34 A`）。判据：以数字开头
   且含 2 个以上数值 token —— 要求 2 个是为了不误伤
   `3.1. Residual Learning` 这类真标题（它只有编号一个数值）。
8. **标题层级改由字号差推断**（章 +2pt → 一级，节 +1pt → 二级），
   编号可用时优先用编号。前端相应保留 h2/h3/h4 三档
   （原先把 level≥2 一律映射到 h3，丢掉了层级信息）。

#### 效果（正文段落中位长度，只统计纯正文行）

| 论文 | 修复前 | 修复后 |
|---|---|---|
| 2015 LeCun | 被切碎 | **472 字符** |
| 2016 ResNet | 被切碎 | **238 字符** |
| 2017 Transformer | 被切碎 | **209 字符** |
| 2022 CoT | 被切碎 | **178 字符** |

ResNet 抽出的结构（实测）：

```
[章] Deep Residual Learning for Image Recognition
[章] Abstract
[章] 1. Introduction
[章] 2. Related Work
[章] 3. Deep Residual Learning
[节]   3.1. Residual Learning
[节]   3.2. Identity Mapping by Shortcuts
[节]   3.3. Network Architectures
[章] 4. Experiments
[节]   4.1. ImageNet Classification
```

#### ⚠️ 顺带修掉一个隐蔽的方法论错误

`tools/make_font_fixture.py`（生成回归夹具的脚本）里**复制了同一套
有 bug 的规则**（`SHORT = 60`）。所以夹具本身是被 bug 切碎的结果 ——
拿它验证新规则等于**用 bug 衡量 bug**（循环论证）。

已把夹具生成器改成与新版一致的规则，并重新生成全部夹具。
这个问题值得单独记下来：**测试数据的生成逻辑必须与实现解耦，
否则测试永远通过**。

#### 工具

- `tools/make_font_fixture.py`：重写（与实现同规则）
- `tools/regen_and_verify.py`：批量生成 + 健康度报告 + 泛化抽样验证
- `tools/health_check.py`：只统计**正文段落**的长度分布
  （排除封面/作者/脚注 —— 它们本来就短，混进来会误判）
- `tools/emit_blocks.py`：从夹具产出结构化 blocks，供前端渲染验证

### 说明：剩余局限

单个数值的表格单元格（`9.15 PReLU-net [12]`）仍会被判成标题。
要准确识别它需要表格结构检测（判断单元格的行列归属），
那是明显更大的一项工作。当前这类行会渲染成小标题，
视觉上可接受，不阻塞阅读。

---

## [0.1.15] - 2026-09-24

### 修改：原始视图改为**连续滚动**，去掉分页

用户 2026-09-24：

> 「pdf 视图显示成功了，但我觉得逻辑有点问题，直接滚动不就好了吗？
> 为什么还要分页？pdf 阅读器不都是滚动的吗？」

完全正确。分页是我在 v0.1.14 自己加的复杂度，代价不小：

- 底部多一整行「上一页 / 页码 / 下一页」，手机上很占地方；
- 每次翻页都要等原生渲染（100-300ms），翻快了会看到空白；
- 想连续看两页交界处的内容要来回按，而滚动是自然的。

主流阅读器默认都是连续滚动，分页只在横屏/双栏这类场景才用。

**改法**：滚动容器一次铺好**全部页的占位**，页图按需加载。

- 先问总页数，为每页建一个占位框；
- 用第 1 页的真实宽高比撑开每个占位框
  （`padding-top: calc(100% * var(--pdf-ratio))` ——
  百分比 padding 相对**宽度**算，所以转屏时高度自动跟着变，
  不用监听 resize，也不需要预先问原生要每页尺寸）；
- 用 `IntersectionObserver` 观察每个占位框，进入视野
  （含上下各约一屏的余量）时才向原生要图；
- 加载过的页 `unobserve`，不会重复回调。

**⚠️ 但「不能一次取全部页」这个判断没变** ——
一页 JPEG 的 base64 约 400KB，12 页就是 5MB 字符串跨桥传递，
必然卡顿甚至 OOM。这正是当初改成分页的原因；
**错的是「所以只能分页」这个推论，不是那个判断。**

实测懒加载行为（12 页文档）：

| 操作 | 实际加载的页 |
|---|---|
| 刚进入 | `[1, 2]` |
| 直接跳到底部 | `[11, 12]` |
| 滚到中间 | `[6, 7, 8]` |

始终只有视野附近的几页在内存里。

配套清理：

- 删掉 `.pdf-pager` 相关样式与 `reader.prevPage` / `reader.nextPage` 文案；
- 新增 `teardownPdfScroll()`：切换视图、打开新文献、关闭阅读页
  三处都**显式 `disconnect()`** 观察器。
  只把变量置 null 是不够的 —— 观察器与占位元素互相持有引用，
  不断开就是一个自留的环，连开十几篇内存会明显上涨。

### 说明：滚动不会误触菜单

滚动是 `scroll` 事件，唤起菜单是 `click`，两者本来就不冲突
（v0.1.14 担心的"抢同一次触摸"只发生在**手势**翻页上，
按钮翻页没有这个问题，改成滚动后更是彻底不存在）。
实测：滚动后菜单状态不变；点中间仍能唤起菜单，且在 PDF 之上。

---

## [0.1.14] - 2026-09-24

### 重构：原始视图改为「原生渲染成图片」，PDF 从此只是网页里的一块内容

**症状**（连续三条用户反馈，指向同一个结构性问题）：

1. 「转换视图直接黑屏了」
2. 「但唤起菜单应该在 pdf 上面啊」
3. （上一条遗留）「没有成功转换视图」「肯定需要顶部栏，不然我们没法换回去」

**根因**：v0.1.12 / v0.1.13 都在迁就「WebView 的内置 PDF 查看器」。

- v0.1.12 让**主 WebView** 顶层导航到 PDF
  → 能渲染，但**整个界面被替换**：顶栏消失、切不回来。
- v0.1.13 改用**覆盖层 WebView**
  → **直接黑屏**，并且照样盖住我们自己的菜单。

黑屏的技术原因：内置查看器必须发 **Range 请求**（HTTP 206 分片）
才能渲染，而 `shouldInterceptRequest` 只能给出一条整段流。

但更根本的问题是**结构**：无论哪种做法，PDF 都成了
「盖住一切的另一层界面」—— 那么顶栏、底栏、设置面板、目录弹窗
就永远在它下面，「在 PDF 里点中间唤起菜单」也就永远不可能实现。

**改法**：不再使用内置查看器，**原生自己把页渲染成图片**。

- 新增 `PdfPages.kt`：用系统 `android.graphics.pdf.PdfRenderer`
  把指定页渲染成 JPEG（宽 1600，按比例），
  缓存在 `cacheDir`（派生数据，不进数据目录配额），
  **先写 .tmp 再原子改名**（直接写目标文件时中途失败会留下半截 JPEG，
  而下次进来 `isFile` 为真就当成缓存命中，永远显示撕裂的图）。
- 桥换成 `getPdfPage(id, page)` / `getPdfPageCount(id)`，
  **同步返回 base64**（网页 `img.src` 要立即拿到值）。
- 网页把页图放进 `.reader-content`，配一条翻页条（上一页 / 页码 / 下一页）。

**于是 PDF 只是网页里的一块内容**，这是本次改动的全部意义：

- 顶栏(11)、底栏(11)、设置面板、目录弹窗**天然在它上面** ——
  不用任何 z-index 技巧，层级由 DOM 结构决定；
- 返回键、菜单、主题、i18n **全部沿用原有路径**，不需要特例分支；
- 不再有「离开网页」这回事，跨 WebView 通信整套机制随之删除。

**同时删除**（都是前两种做法的残留）：

- 布局里的第二个 WebView（`raw_view`）
- `configureRawView()`、`isShowingRawPdf()`、`closeRawView()`、
  `notifyRawClosed()`、`rawPdfUrlFor()`、`openRawPdf()`
- `pdfResponseFor()` 与 `PDF_URL_PREFIX`（HTTP 通道整套）
- 返回键里「先判是不是在看 PDF」的分支

**取舍说清楚**：翻页由我们自己做（按钮，不做滑动手势 ——
滑动翻页会在 WebView 里和「点中间唤起菜单」抢同一次触摸）。
一次只渲染当前页，因为一页 JPEG 的 base64 约 400KB，
12 页一次全取就是 5MB 字符串跨桥传递，必然卡顿。

### 修改：原始视图的字号/配色不再套用正文样式

页图撑满宽度（负外边距抵消正文的 24px 留白与状态栏让位），
底色用 `--bg`，与网页深色底一致。

---

## [0.1.13] - 2026-09-24

### 修复：切到原始视图后**切不回来**，且切换时"什么也没变化"

**症状**（用户反馈）：
1. 「没有成功转换视图，什么也没有变化」
2. 「肯定需要顶部栏，不然我们没法换回去」
3. 「你怎么还是做了阴影？我说了只转换图标本身」

**根因（v0.1.12 的设计缺陷）**：

v0.1.12 把 PDF 装进了**主 WebView**（顶层导航）。这确实让 PDF
渲染出来了，但代价是**整个应用界面被替换掉** —— 顶栏跟着消失，
用户没有任何入口切回阅读视图。也就是说：修好了"看不见"，
却造出了"出不去"。

网页侧的按钮图标虽然切成了"原始视图"，但用户已经看不到顶栏了，
观感就是"点了没反应"。

**改法**：另开一个 WebView（`raw_view`）专门承载 PDF，盖在网页之上。

- PDF 是**它**的顶层文档 → 内置查看器肯渲染（v0.1.12 的成果保住）
- 主 WebView 与我们的网页**完好无损** → 顶栏还在，随时能切回来
- 退出：原生的返回键把覆盖层隐藏，并通知网页把图标状态归位

配套改动：

- 新增 `configureRawView()`。配置**刻意比主 WebView 少**：
  不加 JS 桥、不加 `WebChromeClient`、不加载 assets。
  它只做一件事 —— 显示 PDF。配置越像主 WebView，
  越容易让人误以为"它也能跑我们的网页"（它不能，它没有桥）。
- ⚠️ 但 `javaScriptEnabled` **必须为 true**：
  内置 PDF 查看器自己就是用 JS 写的，关掉它渲染出来就是**一片空白**
  （实测）。这看起来反直觉，别"顺手"改成 false。
- `shouldInterceptRequest` 里只接 `pdfResponseFor`，
  **其余请求一律返回 null**（不拦截）—— 查看器会注入自己的资源，
  若回落到 `assetLoader` 而它对未知路径返回 null，
  查看器会被当成"资源不存在"从而渲染不出来。
- `closeRawView()` 里 **`loadUrl("about:blank")` 是必需的**，
  不只是 `visibility = GONE`：只隐藏的话查看器仍持有 PDF 文件句柄
  与渲染资源，连读十几篇内存明显上涨（隐藏 ≠ 卸载）。
- `isShowingRawPdf()` 的判据从"主 WebView 的 URL 是不是 /pdf/"
  改为"覆盖层是否可见" —— 主 WebView 的 URL 现在永远是入口页，
  用旧的判据**永远返回 false**，返回键会直接退出应用（实测踩过）。
- 回到阅读视图时**不再**需要额外的"归位"逻辑走主路径 ——
  网页从未被切走。

### 修改：视图切换按钮**只换图标**，不换底色

v0.1.12 加了一条 `[aria-pressed="true"] { background: ... }`，
按下时按钮出现一块底色。用户明确要求去掉：

> 「二者类似于一个状态，所以不要有按钮的点击特效，直接切换图标就行」
> 「你怎么还是做了阴影？我说了只转换图标本身」

这两只图标（`visibility` / `visibility_off`）是**同一状态的两个面**，
不是两个按钮。底色的出现会把顶栏切成"一个个按钮"，
与它作为阅读页工具条的定位不符。现在 `[aria-pressed="true"]`
与 `:active` 一并被压成透明。

⚠️ `aria-pressed` **没有删** —— 它对读屏器仍然有用，
只是 CSS 里不给它任何视觉。现在的状态表达完全靠**图标本身**：
眼睛加斜杠 = 正在看 PDF；正常眼睛 = 正在看阅读视图。

---

## [0.1.12] - 2026-09-24

### 修复：原始视图完全看不见 PDF

**症状**：切到原始视图后画面是**空的**。（用户反馈）

**根因**：网页里放的是 `<iframe src=".../pdf/<id>">`。
实测 iframe 尺寸正常（840×412）、URL 也真发出去了，但**不渲染**。

Android WebView 的内置 PDF 查看器是为**顶层文档**设计的 ——
它在子框架里不工作。这是查看器的行为，不是我们的代码错；
之前把它当普通资源来嵌，方向就错了。

**改法**：改为**原生接管** —— 切到原始视图时，网页把文献 `id`
交给原生，原生对 **WebView 本身**做一次 `loadUrl`
（等同用户在浏览器里直接打开一个 PDF 链接）。

配套改动：

- 新增桥方法 `openRawPdf(id)`。只传 **id 不传 URL** ——
  URL 的拼法（主机名、`/pdf/` 前缀）是原生侧知识，
  让网页自己拼等于复制两份。
- 返回键：PDF 是全屏导航，网页此时不在前台（问它也不会答）。
  所以原生**先**判断「当前是不是 PDF 通道」，是就 `goBack()` 回网页，
  再按才关阅读页。
- 预览（无原生桥）下**不能停在空白页** ——
  回退到阅读视图并提示，而不是装作切成功了。

⚠️ 代价说清楚：在 PDF 里时看不到我们的顶栏/选项栏
（查看器自带工具栏，里面有返回）。这个取舍是刻意的：
**能看见了**比“顶栏一直在”重要得多。

### 修改：图标换成 visibility / visibility_off，并去掉点击特效

用户反馈：`raw_on` / `raw_off` **比原来的小、反而不明显**，
改用 `visibility` / `visibility_off`。

| 视图 | 图标 | 含义 |
|---|---|---|
| 阅读 | `visibility` | 「把原始视图显示出来」 |
| 原始 | `visibility_off` | 「原始视图已显示，点它收起来」 |

另：「二者类似于一个状态，所以不要有按钮的点击特效，
直接切换图标就行」—— 按钮的**按压反馈已去掉**
（按下去的底色变化/缩放会让人以为“操作正在进行”，
而对一个开关来说图标变了就是最好的反馈）。

> ⚠️ 实现细节：这条样式必须写成 `.icon-button.reader-view-toggle`
> （两个类）。只写 `.reader-view-toggle` 会被 `.icon-button:active`
> 盖掉 —— 同优先级 (0,2,0) 而后者定义在文件更后面。实测就是
> `transition` 仍是 `background-color`、底色照旧变化。
> 选中态那条则必须紧跟在它**之后**（同优先级靠源序胜出）。

---

## [0.1.11] - 2026-09-24

### 修改：视图切换改用 raw_on / raw_off 图标，术语定为「阅读 / 原始」

用户找到了更贴切的图标对，两个视图的**命名**也随之定下：

| 视图 | 含义 | 图标 |
|---|---|---|
| 阅读视图（reading） | 重排后的正文，用来**读** | `raw_on` |
| 原始视图（raw） | 出版方原始版面，用来**核对** | `raw_off` |

图标本身写着 RAW，用「有无斜杠」区分开关 —— 比抽象的 eye 图标
更直白，不靠文字解释。

#### 术语为什么不用「文本 / PDF」

`PDF` 是**文件格式**，不是用户的意图。用户要做的事只有两件：
读正文、核对原始版面。命名跟着**意图**走，
切语言时也不会变扭（"PDF 视图" 算不上一个中文说法）。

#### 图标与标签都描述**动作**

```
在阅读视图 → 显示 raw_on   + 标签 "Raw View"
在原始视图 → 显示 raw_off  + 标签 "Reading View"
```

两者**必须同向**。曾经把标签写成动作、图标写成当前状态 ——
按钮在说两件事，谁看都会错。

⚠️ 那「当前在哪个视图」由谁表达？`aria-pressed`（唯一的状态出口），
不靠图标/标签重复。处在原始视图时按钮底色变深。

#### 两只图标预置，只切 `hidden`

两个 `<span>` 一次性建好，切换时只切 `hidden` 属性，
**不重建 innerHTML** —— 重建会让图标闪一下（要重新解析 SVG）。

> ⚠️ `hidden` 对 `<span>` 需要 CSS 兜底：UA 的 `[hidden]{display:none}`
> 优先级很低，会被把 span 设成 flex 的规则盖掉，
> 结果两只图标同时显示、挤在一起。

### 新增工具：`tools/fetch_icon.py`

把官方 Material Symbols 的 `viewBox` 与 `path d` 打出来。

```powershell
python tools/fetch_icon.py raw_on
```

⚠️ 不要用 `fetch_webpage` 拉图标 —— 它会把 SVG 当**图片**渲染，
看不到路径文本。这个脚本直接读纯文本并按需打印。

---

## [0.1.10] - 2026-09-24

### 新增：阅读页第二个视图（原始版面 ↔ 阅读）

> ⚠️ v0.1.11 已把命名与图标改定：两个视图叫**阅读视图 / 原始视图**，
> 图标换成 `raw_on` / `raw_off`。本节保留当时的描述以便追溯，
> 术语以 v0.1.11 那节为准。

顶栏详情按钮**左边**多了一个按钮，用来在两个视图间切换：

| 视图 | 内容 |
|---|---|
| 阅读（默认） | 重排后的正文（v0.1.6 的结构化渲染） |
| 原始 | 出版方原始版面，带 WebView 内置查看器（缩放/翻页原汁原味） |

图标取自官方 Material Symbols，经 `tools/fetch_icon.py` 拉取而非手写。
处在原始视图时按钮高亮，无障碍标签会变成「切到另一个视图」的名字。

#### 底层：给网页开一条**受控**的 PDF 通道

PDF 存在应用私有目录，而 WebView 的 `allowFileAccess = false` ——
**网页原本根本拿不到这个文件**。现在由原生拦截
`https://appassets.androidplatform.net/pdf/<id>` 并按需**流式**返回。

选这个方案而不是其他三个（代码里有完整记录）：

| 方案 | 为什么不选 |
|---|---|
| 给 `file://` 路径 | 需要打开 `allowFileAccess`，等于向网页暴露整个私有目录 |
| 读成 base64 喂 pdf.js | 20MB 的 PDF 转 base64 约 27MB 字符串，**会 OOM**；PDF 必须流式 |
| 起 localhost HTTP 服务 | 要占端口、管生命周期，多一个对外监听入口 |
| **`shouldInterceptRequest`**（采用） | 文件仍在私有目录，按需流式；权限只在这一处把关 |

并且用 WebView 的**内置查看器**渲染，不引入 pdf.js
（省几百 KB 与一份第三方代码）。

⚠️ `id` 直接来自 URL，所以做了白名单校验（`[A-Za-z0-9-]`）——
不做的话 `pdf/../../x` 这类路径穿越能读到私有目录里的其它文件。

⚠️ 只在真正切到 PDF 视图时才设 `iframe.src`（PDF 动辄几十 MB）；
关闭阅读页时置 `about:blank` 真正释放内置查看器
（隐藏 != 卸载，否则连读十几篇内存会涨）。

### 修复：更新弹窗文案换行 → 一行解决

用户要求「尽量做到一行文字解决」。

旧文案是两句：

```
Version 0.2.0 has been released (14.7 MB). You are on 0.1.9.
```

实测 **360dp 屏（正文框 320px）下换行成两行**。改为：

```
Version {version} ({size}) is ready.
```

取舍：去掉「has been released」（标题已写 Update Available），
也去掉「You are on {current}」（当前版本在设置 → Version 里看得到）。
保留 `size` —— 用户想知道要下载多少流量。

> ⚠️ 顺带清掉了 `update.js` 里对已不存在的 `{current}` 的替换 ——
> 它匹配不到（无害），但会让后来人以为文案里还有当前版本。

### 修复：设置页底部留白不足

`#page-profile` 的 `padding-bottom` 是 **0**，最后一组（Sign Out）
正好被底部导航栏压住。改为 `calc(80px + var(--safe-bottom))`
（64 导航 + 16 呼吸，再叠手势条安全区）。

### 修复：设置页分组标题与选项之间间隔过大

`.setting-group-title` 旧值 `14px / 6px` 上下内边距，叠上
`.setting-group` 的 `4px 0` 与 18px 行高 —— 一个 12px 小标签
前后占了 **42px**（实测标题盒高 38px）。

改为 `10px / 2px`：上方留一点与上一组分开，下方**紧贴自己的列表**
（相邻原则：标题必须明显更靠近它标注的那组）。

### 新增：本地图标拉取工具

`tools/fetch_icon.py <icon-name>` —— 把官方 Material Symbols 的
`viewBox` 与 `path d` 打出来，避免凭记忆手写图标路径
（历史上因此出过三次事故）。

---

## [0.1.9] - 2026-09-24

### 修复：设置项「上面有线下面没有」

**症状**：设置页的分组卡片，**顶部有一条线、底部没有**，
看起来像被切掉一截。（用户反馈：「要么都有，要么都没有才行」）

**根因**：`.setting-list` 只有 `border-top`，而项与项之间的分隔线
来自每个 `.setting-item` 的 `border-bottom`（最后一条由 `:last-child` 去掉）。
于是三条边里只画了两条：

```
列表顶部   ← .setting-list 的 border-top        （有）
项与项之间 ← .setting-item 的 border-bottom      （有）
列表底部   ← 谁都没有                            （空）★
```

**改法**：选「**都没有**」—— 去掉 `.setting-list` 的 `border-top`，
只保留项与项之间的分隔线。

> 为什么不选「都加上」：补一条 `border-bottom` 会让最后一行内容
> 多出一条紧贴圆角的下边线，与「卡片式分组」（`--surface` 底 +
> 16px 圆角）的语言冲突 —— 卡片的边界靠**底色差**表达，不靠描边。

### 修复：账户卡片同一个问题

`.account-card`（个人页顶部头像/用户名那张卡）也只有 `border-top`，
同样去掉。这是个**未被报告**的同类缺陷 —— 顺着用户指出的现象
把同类写法都查了一遍（`grep border-top`）。

- ⚠️ `.reader-panel` 的 `border-top` **保留**：它是贴屏幕底部的弹层，
  那条线的作用是「与上方正文分隔」，语义不同，不是框。

---

## [0.1.8] - 2026-09-24

### 修复：日期透明输入框冒出一条位置不对的竖线

**症状**：日期填到第 5 位时，**第一个框右边冒出一个输入符号**；
到第 6 位消失，之后不再出现。（用户反馈）

**根因**：透明 `<input>` 铺满整行，它按**自己的排版度量**
从左边缘依次摆字符（实测 7.5px/字）；
而展示层的 8 个格子是「48px 宽 + 8px 间隔 + 两个 `-` 分隔符」。
两套位置**根本对不上**，于是 caret 会落到格子之间或边框上：

| 已填位数 | caret x | 落在 |
|---|---|---|
| 4 | 30 | 第 1 格内 |
| **5** | **37.5** | **第 1 格偏右 —— 就是"多出来的符号"** |
| 6 | 45 | 第 1 格右边缘 |
| 7 | 52.5 | 格间空隙（看不见） |
| 8 | 60 | 第 1 格右侧外 |

**改法**：把 caret **彻底隐藏**（`caret-color: transparent`），
焦点位置改由**高亮「下一个字符会落到的那一格」**来表达，
并在失焦时撤掉。

> ⚠️ 没有采用「用 `letter-spacing` 把 caret 对齐到格子」——
> 那要把分隔符宽度和 gap 一起凑，换个字体或 WebView 就错位，
> 是个永远修不干净的活。

### 改进：列表卡片年份更突出

用户反馈「右下角的年份字体有些不够突出」。

年份改为 **600 字重**（同字号 11px），颜色提到 `--on-surface` + 0.85 不透明度。

- **不加新颜色档位**：卡片层级约定是「字号 + 字重分层，
  颜色只有亮（标题）/ 淡（其余）两档」——
  作者栏曾因与标题同色被否掉，给年份单独加一档会破坏这条约定。
- 年份比「页数」更值得突出：页数是「文件多厚」，年份是**筛选文献的主要依据**。

---

## [0.1.7] - 2026-09-24

### 修复：详情页日期输入卡在第二位（真机专属）

**症状**：进入文献详情页，在日期那行输入数字，**只有第二位会变** ——
后续输入的每个数字都只更新第二格，整行再也不前进。

**根因**：日期原先是 **8 个真的单字 `<input>`**，每格自己监听 `keydown`
决定「这一位往哪写、要不要把焦点推到下一格」。
但 **Android 软键盘输入数字时不派发带字符的 `keydown`** ——
事件是 `keydown`(`key=Unidentified`/`229`) + `input`（输入法上屏）。
于是那些推进逻辑**在真机上从不执行**，字符被浏览器默认插进当前聚焦的格，
`input` 分支只看到 1 个字符、只做同步、**不推进** —— 第二格越攒越长
（`"20"`、`"203"`…），整行卡死。

> 桌面浏览器 `keydown` 正常，所以此前在 PC 上测不出来，
> 表现为「只有真机才有」。

**改法**：把日期行从「8 个真输入框」改为
**「一个铺满整行的透明 `<input>` + 8 个纯展示的 `<span>` 格子」**。

- 「哪一格」不再靠事件推进，而是**由字符在值里的下标决定**；
- 因此 `keydown` / `input` / `beforeinput` / 粘贴 / 输入法 / 语音输入
  **全部走同一条路径**，天然正确；
- 展示层 `pointer-events: none`，绝不挡住点击
  （否则点哪一格都没反应 —— 与 z-index 覆盖是同类陷阱）。

**输入形式与存储格式完全没变**（外观仍是 8 个圆角框、值仍是
`2015` / `2015-06` / `2015-06-24`），**老数据无需迁移**。

| 输入 | 存储 |
|---|---|
| `2015` | `2015` |
| `201506` | `2015-06` |
| `20150624` | `2015-06-24` |
| `201` | `""`（年不完整 → 清空，**绝不存错值**） |
| 粘贴 `2015-06-24` | `2015-06-24`（连字符自动剥掉） |

顺带修正：日期字段在保存时改为直接读 `draft`（它的 `<input>` 里
存的是纯数字串，走通用分支会把 `2015-06-24` 存成 `20150624`）。

### 新增：本地无缓存预览服务器（工具）

`tools/serve_nocache.py` —— Python 自带 `http.server` 会发
`Last-Modified`/`ETag`，浏览器对子资源返回 **304**，
于是**改了 JS 但页面还跑旧代码**，排查时会误以为「改动没生效」。
该脚本直接发 `no-store`，杜绝此问题。

```powershell
python tools/serve_nocache.py 8903 app/src/main/assets/www
```

---

## [0.1.6] - 2026-09-24

### 新增：正文结构化渲染（原生 → 网页）

原生侧产出结构化 `blocks`（`heading` / `paragraph` / `formula`），
前端按 `kind` 渲染，不再把整页当一坨文本。

### 修复：「标题窜进正文」

双向 flush —— 到达的行**像标题**时也先收掉上一段。

### 修复：公式碎片散落

单个符号（`/`、变量孤字如 `x yl zk`）拉到相邻块；
顺序要紧凑 —— **页码先独立成块**，否则 `13.5` 会黏住页眉 `14`
产出伪标题。

### 修复：断词续行

以连字符结尾的行不再当标题；合并时去掉连字符
（`re-` + `search` → `research`；保留 `machine-learning` 这类真连字符）。

### 修复：空格规则

只在**两侧都是字母/数字**时补空格（`x=` 而非 `x =`）。

### 输入体验

- 日间模式对比度：真值提升到 `--on-surface`，占位保持 `--on-surface-variant`
- `.detail-input::placeholder` 之前**缺失** → 提示与真值同色（用户会以为已填）
- 日期格边框不再硬编码 `rgba(51,51,51,.7)`，新增 `--outline-faint` 主题令牌
- 两行输入框标题栏 / 只读值 / 日期格占位统一走令牌，日夜间都正确

---

## [0.1.5] - 2026-09-24

### 新增：文献详情页（元数据编辑）

点阅读页右上角的「详情」按钮进入。**所有字段都可留空** ——
PDF 抓不到的值留空即可，不设必填、不阻止保存。

| 类别 | 专属字段 |
|---|---|
| 期刊 | 期刊名 / 卷 / 期 / 起始页 / 终止页 / DOI |
| 会议 | 会议名 / 简称 / 地点 / 页码 / DOI |
| 预印本 | 平台 / 编号 / DOI |
| 专著 | 出版社 / ISBN / 版次 |
| 学位论文 | 机构 / 学位类型（下拉） |
| 报告 | 机构 / 报告编号 |
| 未判定 | 仅通用字段 |

通用字段：标题 / 作者 / 日期 / 短标题。**发表物类别是第一项** ——
选定后下面的字段表跟着变（Zotero 的逻辑）。

### 新增：短标题（Short Title）

填了之后**顶替列表卡片①行的标题**。与「发表物简称」**不是一回事**：

- **短标题** = 这一篇文献标题的短形式，一篇一个值，存在 `doc.venueShort`；
- **发表物简称** = 会议/期刊名字的缩写（NIPS、CVPR），
  是「设置 → 发表物简称」里的全局映射表，作用在卡片③行。

英文标签用 `Short Title` 而非空泛的 `Short Name`
（`shorttitle` 是 BibTeX / Zotero 的标准字段名）。

### 新增：DOI 结构化输入

DOI 有固定语法（ISO 26324）：`10.<注册机构号>/<后缀>`。
输入框据此拆成两段，`10.` 作为不可编辑的装饰前缀：

```
10. [ 1038 ] / [ nature14539 ]
```

存储值仍是完整串（`10.1038/nature14539`），**老数据无需迁移**。

> ⚠️ 粘贴完整 DOI（含 `https://doi.org/...` 或 `doi:` 前缀）到任一格时，
> 会先解析再**分发到两格**。早期只做「剥掉非数字」会把斜杠删除，
> 静默损坏成 `10.10103814539`。

### 新增：`PdfMeta` 深度元数据提取

- **年份**：从 XMP `xmp:CreateDate` 与 `Subject`/`Keywords` 提取，
  1900–当前年之间才算合法（防把卷号、页码当年份）。
- **发表物类别**：按关键词判定 journal / conference / preprint / book /
  thesis / report，判不出归 `unknown`。
- **载体名清洗**：剥掉 ACL 分类串（`[en.us]` 之类）、URL、超长噪声。

### 修复：卡片载体名永远不显示用户配的简称

`vault.js` 读的是 `doc.venueShort`（那是**短标题**），
而设置里的映射表由 `ScholariusShortcut.lookup()` 提供 ——
**这个函数定义了却从没被调用过**，用户在设置里配的简称永远不生效。
已改为调 `lookup()`。

### 修复：搜索不到短标题

短标题顶替卡片标题后，屏幕上显示的是它，但 `matches()` 只搜 `doc.title` ——
**看得到的字搜不出来**。已把 `doc.venueShort` 加入搜索范围。

### 修复：详情页各项间距不一致

用户反馈「类型文字与短标题文字的距离，和短标题与标题文字之间的间隔不一样」。
实测 **31px vs 20px**（差 11px）。根因是**间距有多个来源互相叠加**：

- Type 行与 Short Title 行各带 `padding: 12px`，在交界处叠成 26px；
- Short Title 行与字段表交界处只有 4px；

且 Short Title 行的输入框带 `padding: 6px 0`（行高 30px vs 23px），
`align-items: center` 把多出的 7px 摊到标签上下，让标签位置随行高漂移。

**改法：相邻两项的距离只能有一个来源。**
行不再自带纵向 padding / min-height；输入框纵向 padding 归零；
间距统一由容器 `gap: 16px` 提供。结果 **16px vs 16px**。

### 修改：排列细节

- 日期输入：**8 个单字圆角框占满一行**（`YYYYMMDD` 逐位提示）。
  连打 8 位、退格回退、粘贴 `2015-06-24` 全部支持。
- 页码拆成「起始页 / 终止页」两个独立输入框（原为单个自由文本框）。
- 短标题输入框**去掉下划线**，改为「左标签 + 右对齐值 + 空值时占位提示」。
- 所有空输入框都有**带真实示例值**的占位提示（`e.g. 521` 而非「卷号」）。
- 「文件页数」改为**单行**（标签左、值右），与其余项等高。

> ⚠️ 「文件页数」原本因 `min-height: 48px` 而虚高 25px ——
> 该值对只占 23px 的单行项是纯空气，正是"有的项松、有的项挤"的观感来源。

### 修复：切语言后占位提示不刷新

标签走 `data-i18n` 属性由 DOM 扫描更新，而 placeholder 是 JS 按当前语言写入的
（不能写死在 HTML，否则切语言不更新）——它不在扫描范围内。
语言切换的监听器漏调了 `syncShortName()`，于是同一行里中英混排。

> 这个 bug 一直存在，只是早期中英的提示串相同（都是 `e.g. NIPS`）看不出来。

---

## 关于 0.0.4 – 0.0.13 的无效迭代（记录在案，勿重犯）

启动页「一闪而过」**真正的唯一原因**是：

```
.splash { z-index: 40 }     ← 被压在下面
.login  { z-index: 50 }     ← 盖住了它
```

一行 `grep z-index` 就能查出，却在 **0.0.4 – 0.0.13 共 10 个版本**里被反复误诊，
其中 **0.0.9 – 0.0.12 的 4 个版本全部是同一错误假设下的无效改动**
（反复调整动画时长与 `animationend` 逻辑），并一度把问题**改得更糟**：

| 版本 | 当时以为的原因 | 真实情况 |
|---|---|---|
| 0.0.4 | `getAnimations()` 误判 | 无关 |
| 0.0.5 | 兜底超时太短 | 无关 |
| 0.0.9 | `updateFlowActive` 死锁 | 无关（那是更新检测的独立 bug） |
| 0.0.10 | splash 阻塞门控 | 无关 |
| 0.0.11 | 动画前 82% 静止 | 无关 |
| 0.0.12 | `animationend` 不可靠，改显式计时 | 无关 |
| **0.0.13** | 加诊断浮层 | **这才拿到决定性日志** |
| **0.0.14** | — | **真正修复（z-index）** |

**教训**：用户报告「看不见」时，先按「可能被别的东西挡住」查（`z-index` / 堆叠上下文 /
`elementFromPoint`），再查逻辑。详见长期记忆的调试铁律。

---

## [0.0.19] - 2026-09-23

### 结论：应用内更新**没有 bug**

v0.0.18 真机日志显示更新检测完全正常：

```
[update] 查询 https://api.github.com/repos/...（当前 0.0.18）
[update] API 返回 HTTP 200
[update] tag_name='v0.0.18'
[update] assets 共 1 个
[update]   asset: Scholarius-v0.0.18.apk
[update] 最新 0.0.18 不大于当前 0.0.18
[update] 结论：没有可用的新版本
```

得到的都是最新版，所以「无可用新版本」是**正确行为**。

之前看到的「无新版本（或查询失败）」把两种情况混在一起，是因为
`WebView.evaluateJavascript()` 必须在主线程调，而更新检查在后台线程、
其日志回调也在后台线程 —— 细节会被静默丢弃。v0.0.18 已修（`debugLog` 自行切线程）。

### GitHub App：有进展，但深链路径不对

v0.0.18 日志：

```
[VIEW+setPackage] 失败：ActivityNotFoundException
  （证实它不接受 https 链接）
[github:// 深链] 成功
已用 GitHub App 拉起 ✓
```

**能弹出「选择打开方式」了（之前完全做不到）**，但列表里没有 GitHub ——
说明 `github://github.com/login/device` 这个 path 不匹配它的规则。

本轮修正与加诊断：

1. 深链改为**裸 `github://`**（不带自己拼的 path），这是最保守、
   能匹配它任何一条 github:// 规则的写法。
2. 新增 `dumpIntentFilters()` —— **把 GitHub App 自己声明的深链规则打出来**，
   照着它的格式构造，而不是猜。

---

## [0.0.18] - 2026-09-23

### 修复

- **GitHub App 仍然只走浏览器（v0.0.17 真机日志已定位）。**

  日志：

  ```
  com.github.android 存在，名称='GitHub' enabled=true   ← <package> 点名生效
  com.github.android getLaunchIntent=ComponentInfo{...}
  能处理该链接的 App 共 1 个
    · com.android.chrome/...                            ← 仍然只有浏览器
  没找到 GitHub App
  ```

  **根因：GitHub App 没有注册 `https` 的 VIEW intent-filter。**
  所以 `queryIntentActivities` 里永远没有它 —— 这条路本身就走不通，
  不是可见性问题（它已经能被看见了）。

  改为**主动进它的包里去开**，逐个尝试三种方式：

  | 方式 | 做法 |
  |---|---|
  | 1 | `ACTION_VIEW` + `setPackage` |
  | 2 | `github://` 自定义深链 + `setPackage` |
  | 3 | `getLaunchIntentForPackage` 拉起主 Activity |

  任一成功即停；全失败才退回浏览器。
  `queryIntentActivities` 降级为仅用于掳第三方 GitHub 客户端。

### 诊断改进

- 诊断浮层改为**可折叠 + 可复制**（用户要求）：
  - 默认折叠成一条 37px 横条，不挡视野；
  - 点「展开」显示日志，**可长按选中复制**；
  - 「复制」按钮一键全选复制、「清空」重置；
  - 折叠状态跳启动记忆。

- `debugLog()` 内部**自行切主线程**。
  `WebView.evaluateJavascript()` 必须在主线程调用，
  而更新检查在后台线程、其日志回调也在后台线程 ——
  不切线程时这些日志被静默丢弃（上一版就是这样：浮层上只看得到主线程打的
  「开始检查」与「无新版本」，中间的 HTTP 状态码、`tag_name`、`assets` 全不见）。

---

## [0.0.17] - 2026-09-23

### 诊断（临时）

v0.0.15 真机日志已定位到 GitHub App 问题的位置：

```
com.github.android 已安装=false        ← getApplicationInfo 查不到
能处理该链接的 App 共 1 个：
  · com.android.chrome/...             ← 只剩浏览器
没找到 GitHub App → 退回浏览器
```

新增 `probePackage()` 区分两种「查不到」：
- `NameNotFoundException` → 真的没装
- `SecurityException` 等 → 包存在但被可见性挡住

并额外输出 `getApplicationLabel`、`enabled`、`getLaunchIntentForPackage`
（后者走另一条不受 `getApplicationInfo` 可见性限制的路径）。

### 同时修复（待验证）

`<queries>` 改为三层声明：

| 层 | 内容 | 作用 |
|---|---|---|
| ① `<package>` | `com.github.android` / `.beta` | 直接点名，不依赖对方注册了什么 filter |
| ② `<intent>` | VIEW + `scheme=https` | 按能力查（浏览器、第三方客户端） |
| ③ `<intent>` | VIEW + `scheme=github` | GitHub App 的自定义深链 |

之前只有第 ② 层，而 `getApplicationInfo()` 查不到说明
**光声明 intent 能力不够**，需要 `<package>` 直接点名。

---

## [0.0.16] - 2026-09-23

### 诊断（临时）

把**更新检测**的日志也接到屏幕浮层上（之前只有 `Log.i`，手机看不到）：

- `Updater.check()` 新增 `onLog` 参数，逐步输出：
  - 请求的 Release API 地址与当前版本
  - HTTP 状态码
  - `tag_name`
  - `assets` 数量与每个 asset 名
  - 版本比较结果（有新版本 / 不大于当前 / 解析失败）
- `maybeCheckUpdate()` 跳过时会打印三个条件的具体值：
  `updateChecked` / `updateFlowActive` / `pageReady`。

装上后打开应用，对屏幕底部截图即可看出为何收不到更新。

---

## [0.0.15] - 2026-09-23

### 诊断（临时）

为排查「GitHub App 没被拉起」，把**原生日志也接到屏幕浮层上**：

- 新增 `MainActivity.debugLog()` —— 同时写 logcat 与网页浮层
  （通过 `ScholariusShell.diag()`）。原生日志本来只进 logcat，
  手机上根本看不到。
- `openDeviceVerification()` 全链路日志：
  - 要打开的 URL、`scheme` / `host` / `path`
  - `com.github.android 已安装=true/false`（区分「没装」与「装了但挑不到」）
  - **枚举出所有能处理该链接的 App**（包名/activity 名）
  - 挑中的 ComponentName
  - 拉起成功 / 失败（含异常类型与消息）
  - 退回浏览器及其结果

装上后点一次「Sign in with GitHub」，对屏幕底部截图即可看出
到底是哪一步断的。

---

## [0.0.14] - 2026-09-23

### 修复

- **启动页「一闪而过」的真正原因：被登录页盖住了。**

  层级写反了：

  | 元素 | 修正前 | 修正后 |
  |---|---|---|
  | `.splash` | `z-index: 40` | **`z-index: 100`** |
  | `.login` | `z-index: 50` | 50 |

  实机日志（v0.0.13）：

  ```
   272ms  splash:timer   | 将在 2600ms 后退场
   427ms  account        | signedIn=false splashDone=false
   431ms  dismiss:blocked| splashDone=false
  2896ms  splash:done    | timer @2624ms
  2897ms  splash:hidden  | @2897ms
  ```

  原生在 **427ms** 就把 `signedIn=false` 推过来，`setAccount()` 立刻
  `show()` 登录页，而那之后 splash 才被收起 ——
  **splash 明明完整显示了 2.6 秒，但 427ms 后就被登录页盖住，用户完全看不见。**

  所以「日志正常」与「用户看到一闪而过」两边都是对的，只是一个在下面、一个在上面。

  现在 `z-index: 100` 凌驾于所有业务层（`.login` 50 / `.row-menu` 71 /
  `.toast` 80）之上。实测：登录页在 597ms 已显示，但 splash 一直盖在上面到 2642ms。

---

## [0.0.13] - 2026-09-23

### 诊断（临时）

启动页「一闪而过」经多轮修复仍未解决，本轮不再靠推断：
把启动时序做成**屏幕上的浮层**，不需要 adb、不需要远程调试。

入口页默认带 `?diag=1`，浮层显示在屏幕底部（`z-index:9999`，
盖过启动页），内容包含：

| 字段 | 含义 |
|---|---|
| `cssDur` / `animName` | 样式表是否生效、动画是否挂上 |
| `sheetApplied` | `<link>` 是否解析完 |
| `atDomReady.position` | 首帧时 `.splash` 是否已是 `fixed` |
| `splash:timer` | 计时器设定的退场时刻 |
| `splash:done` / `splash:hidden` | 实际退场时刻与原因 |

安装后打开应用，直接对屏幕底部截图即可。
定位完会连同 `trace()` 一起移除。

---

## [0.0.12] - 2026-09-23

### 修复

- **启动页退场改为显式计时，彻底不再依赖 `animationend`。**

  之前几版都用 `animationend` 决定退场时机，而这个事件在 WebView 里
  有三个不可靠之处：
  1. 样式表未生效时 `animation-duration` 为 `0s`，事件**立即**触发；
  2. 子元素（`.splash-logo` / `.splash-name`）的动画事件会冒泡上来，
     必须靠 `event.target` 过滤，这个过滤在部分机型上不可靠；
  3. CSS 动画的起点是**元素渲染时刻**，而脚本执行时刻晚于它，
     两者不同步让「已播时长」算不准。

  现在改为：`t=0` 开始计时 → `SPLASH_DURATION_MS`（默认 2600ms，
  并与 `getComputedStyle` 读到的实际动画时长对齐）后退场；
  再加一截兜底余量，**但绝不允许早于动画时长收起**。

  实测：splash 稳定显示 2.48s 后才收起。

- **首帧兜底样式内联进 `<head>`**：`styles.css` 到位之前，
  `<div class="splash">` 只是无样式普通 div（没有 `position:fixed`、
  不覆盖屏幕、不可见），用户会看到「空白 → 登录页」而不是启动页。
  现在把决定性的定位/尺寸/背景内联，首帧一定是满屏黑底居中内容，
  与系统启动页的纯黑底无缝衔接。

---

## [0.0.11] - 2026-09-23

### 修复

- **启动页「一闪而过」的真正手感问题**：前 82% 的时长是**静止的**。

  逐帧实测旧动画：

  | 时刻 | 状态 |
  |---|---|
  | 0–520ms | logo 放大落定 |
  | 160–780ms | 名称淡入 |
  | **780–1835ms** | **完全不动** |
  | 1835–2183ms | 淡出 |

  也就是说 2.2s 里有 **1 秒多屏幕上一动不动**，用户自然觉得
  「启动页早就没了」→ 感知为「一闪而过」。

  重做时间轴（总长 2600ms，全程都有变化）：
  - 0–720ms　logo 从 1.7 倍放大落定到 2 倍
  - 700–1420ms　名称淡入上移
  - 1420–1870ms　停留，让品牌被看清
  - 1990–2570ms　整体淡出

- 启动页退场不再用 `visibility` 关键帧（与 `hidden` 属性职责重叠，
  且会让「动画结束」与「元素消失」两个时机变得含糊）。

- `onPageFinished` 里的三步（推版本号 / 推账号 / 检查更新）改为**互相隔离**。
  原来顺序直调，只要前面任一步抛异常（典型是覆盖安装后
  `EncryptedSharedPreferences` 密钥失效，`auth.isSignedIn` 会抛），
  后面的**检查更新就不会执行**，且没有任何错误提示 ——
  表现就是「永远收不到更新通知」。

- `webPainted` 改为多重触发（`onPageStarted` / `onPageCommitVisible` /
  `onPageFinished` / 4s 定时器）。上一版只靠 `onPageCommitVisible`，
  而这个回调并非在所有情况下都会触发 —— 一旦不触发，
  系统启动页会**永远挡在最上层**，用户完全进不去应用。

---

## [0.0.10] - 2026-09-23

### 修复

- **登录成功也进不去（卡在登录页）**：启动动画阻塞了界面切换。

  `applyGate()` 原本要求「splash 演完 **且** 登录状态已知」两个条件。
  只要动画事件因任何原因没到达，`splashDone` 永远是 `false` →
  `applyGate()` 永不执行 → **登录成功了应用外壳也不显示**。

  正确分工：
  - 界面切换（功能层）→ 只看 `signedIn`
  - splash 退场（视觉层）→ 只看动画

  两者各自独立，互不阻塞。视觉遮罩不该拥有卡住功能的权力。

- **登录时按钮消失导致无法操作**：`onCode()` 里把按钮
  `hidden = true` 只留「取消」。一旦自动跳转失败（GitHub App 拉不起来、
  浏览器也没起、或用户跳过去又退回），**再无任何入口可重开授权页**。

  改为按钮文字变成「打开授权页」，点击重新跳转；并加 15s 启动超时保护 ——
  原生沒回调时恢复按钮而不是永久禁用。

---

## [0.0.9] - 2026-09-23

### 修复

- **永远检测不到更新（死锁）**：`updateFlowActive` 一旦卡在 `true` 就再没人清。

  复现路径：点更新 → 下载成功 → `Updater.install()` 返回 `ERROR_PERMISSION`
  （没给「安装未知应用」权限）→ 把用户送去系统设置页，同时
  `updateFlowActive` **保持 `true`**（弹窗还开着）。
  但用户回来后如果没点弹窗、或直接退出重进 app，这个 `true` 就没人清 ——
  之后每次 `maybeCheckUpdate()` 都在第一行被它挡住：

  ```kotlin
  if (updateChecked || updateFlowActive || !pageReady) return
  ```

  修法两道：
  1. `onResume` 里若发现离开超过 `UPDATE_FLOW_RESUME_GRACE_MS`（2s），
     判定为「流程已中断」并清掉残留状态。
  2. 手动点「版本」行时显式清掉残留状态，保证用户永远有一个
     **一定能突破死锁**的入口。

  > 这就是为什么装了 v0.0.7 也收不到 v0.0.8 的更新提示。

---

## [0.0.8] - 2026-09-23

### 修复

- **启动页一闪而过（找到根因）**：`installSplashScreen()` 的返回值被丢弃了，
  没调 `setKeepOnScreenCondition`。

  系统 splash 的默认行为是**画完第一帧就立刻退场**。但那一帧画的是
  「还没加载完的 WebView」（空白），网页里的启动动画此时根本还没渲染 ——
  于是用户看到的是：黑屏一闪 → 直接就是登录页，网页动画被整个跳过。

  现在用 `setKeepOnScreenCondition { !webPainted }` 把系统 splash
  **留在屏幕上**，直到网页真正画出第一帧（`onPageCommitVisible`）才交棒，
  两者无缝衔接，网页的 2.2s 动画完整可见。

  （之前几版都在调网页侧的 `animationend` / 兜底时长，方向错了 ——
  问题不在网页动画，而在它前面那段没人管的空白期。）

- **GitHub App 仍然只走浏览器**：`setPackage()` 与 App Links 冲突。

  GitHub App 对 github.com 用的是 App Links（`android:autoVerify="true"`），
  系统只有在**不指定包名**、走完整验证流程时才会把链接交给它。
  一旦 `setPackage("com.github.android")` 锁定包名，系统就改成
  「在该包内找能处理这个 intent 的 activity」—— 而它的 activity 只声明了
  autoVerify 的 App Links，没有普通 BROWSABLE filter → 找不到 → 退回浏览器。

  改为用 `queryIntentActivities` 返回的 **ComponentName** 直接启动，
  绕开包名锁定。同时补上「包已装但不在候选里」的诊断日志。

---

## [0.0.7] - 2026-09-23

### 诊断

- 为定位两个真机问题加了全链路日志（**定位完会移除**）：

  | 日志前缀 | 内容 |
  |---|---|
  | `[web] splash:setup` | `readyState`、`cssDur`、`animCount`、`prefers-reduced-motion` |
  | `[web] splash:done` | 退场原因（`animationend` / `delayed` / `fallback`）与耗时 |
  | `[web] dismiss:blocked` | 退场被阻塞时 `splashDone` / `signedIn` 的值 |
  | `[update] ...` | 请求 URL、HTTP 状态码、版本比较结果、跳过检查的原因 |
  | `[native] ...` | splash 兜底超时、`finishSplash` 是否生效 |

  查看方式：`adb logcat -s Scholarius`

### 说明

- **应用内检测不到更新是预期行为**：v0.0.5 及更早版本的更新弹窗关闭链路有缺陷
  （依赖一个不存在的 `scrim` 元素，`closeUpdateFlow()` 从不被调用），
  一旦弹过一次窗就再也不会检查。已在 0.0.6 修复。
  从 0.0.5 升级到 0.0.6 需要**手动安装一次**，之后自动更新才可用。

---

## [0.0.6] - 2026-09-23

### 修复

- **登录只打开浏览器、从不拉起 GitHub App（找到根因）**：
  Manifest 里**没有 `<queries>` 声明**。

  登录时要「优先用 GitHub App 打开授权页」，靠的是 `queryIntentActivities()`
  列出所有能处理该 https 链接的 App。
  **Android 11（API 30）起，除非显式声明可见性，`queryIntentActivities()`
  只能看到系统浏览器这类默认可见的包** —— GitHub App 会被直接过滤掉，
  于是永远挑不到它，每次都退回浏览器。

  这与「包名写错」无关，是包可见性限制。现在声明了
  `<intent><action VIEW /><data scheme="https" /></intent>`，
  不使用 `QUERY_ALL_PACKAGES`（敏感权限，上架需额外说明，没必要）。

- 登录页布局（用户 2026-09-23 要求）：
  - 整块内容上移（改为从 `14vh` 往下排，不再垂直居中）；
  - 提示文字改为 `Sign in with GitHub to Continue`；
  - **错误提示从按钮上方移到按钮下方** —— 放上面会把按钮往下推，
    重试时按钮位置跳动，反而不好点。

### 诊断（临时）

- 启动页「一闪而过」到 v0.0.5 仍未解决，本轮**不再靠猜**：
  在网页侧埋了时序日志（`app.js` 的 `trace()` + `WebAppBridge.trace()`），
  输出到 logcat 的 `Scholarius` tag。
  装上后跑一次 `adb logcat -s Scholarius` 即可看到
  CSS 动画时长、`animationend` 实际到达时刻、退场被阻塞在哪个条件。
  定位完成后会移除。

---

## [0.0.5] - 2026-09-23

### 修复

- **下载完不变安装（根的因）**：`Updater.install()` 完全没检查「安装未知应用」权限。
  Android 8+ 上只在 Manifest 声明 `REQUEST_INSTALL_PACKAGES` 是不够的，
  没授权时 `startActivity` 不会有任何反应 —— 安装器根本不出现，
  用户看到的就是「点了更新、下载完了、然后什么都没发生」。
  现在先查 `canRequestPackageInstalls()`，没权限就送到授权页并回 `permission`。
- `Updater.install()` 的参数从 `Context` 改回 `Activity`，
  并去掉 `FLAG_ACTIVITY_NEW_TASK`。用 ApplicationContext 启动安装器
  在部分 ROM 上会被直接拒绝。
- 已下载的 APK 改为缓存 `File` 对象本身。原来靠版本号重新拼路径，
  等于把 `Updater` 的内部目录规则拄一遍，两边一旦不同步就会
  拿着不存在的文件去拉起安装器。
- `updateFlowActive` 不再在下载完成时就被置 `false`；
  此时安装器还没起来，提前置 false 会让 `onStop` 误判，
  从安装器回来自动重弹一次。
- 补 `downloading` 重入守卫，防止连点「更新」重复下载。
- 补 `onStop()`：退到后台时重置「本次已查过」标记
  （安装中 / 已下好包的情况除外）。
- 弹层关闭改为通过 `openSheet(sheet, onDismiss)` 回调收尾。
  原来依赖一个**根本不存在**的 `scrim` 元素，监听从未注册，
  `closeUpdateFlow()` 永远不会被调用 —— `updateFlowActive` 会
  一直停在 `true`，之后再也不弹更新提示。

### 样式

弹层与表单按钮全面对齐 Livolog：

| 项 | 修正前 | 修正后 |
|---|---|---|
| 弹层定位 | 四边 8px 的浮起卡片 | 贴底整宽、仅上圆角 20px |
| 最大高度 | 无限制 | `calc(100dvh - ...)` + 滚动 |
| 入场动画 | `translateY(12px) scale(.98)` + 透明度 | `translateY(100%)` 滑入 |
| 标题 | 左对齐普通标题 | 居中 + 全大写 + 字距 0.5px |
| 按钮 | 填充胶囊（有底色、圆角 12px） | 纯文字按钮（无底无边框） |
| 按钮排列 | 靠右聚集 | Cancel 靠左 / Confirm 靠右 |
| 进度条 | `hidden` 无效，永远显示 | 补 `[hidden]` 规则 |
| 进度条尺寸 | 6px 高、160ms | 4px 高、200ms |

- 补上缺失的 `--success` 变量（亮 `#15803D` / 暗 `#7EE2A8`），
  这是 `.btn-primary` 的确认色，「确定」类按钮之前一直是错的颜色。
- `.btn-login` / `.btn-ghost` 显式复位 `text-transform` 与 `letter-spacing`，
  避免被表单按钮的「全大写」形态污染。

---

## [0.0.4] - 2026-09-23

### 修复

- **启动页一闪而过（真正原因）**：`v0.0.3` 里为了「处理动画已结束的情况」
  引入了 `splash.getAnimations().length === 0` 的判断，却在 `DOMContentLoaded`
  时机执行 —— 那时 CSS 可能还没应用，`getAnimations()` 返回空数组，
  于是被误判成「动画已结束」，启动页被立刻跳过。
  现在只用 `animationend` 事件判断，并把兜底超时从 3000ms 提到 3400ms
  （必须大于 CSS 里的 2200ms，否则会抢在事件前把启动页收掉）。

  > 这是 v0.0.3 引入的回归。教训：用「探测当前状态」代替「等事件」很危险，
  > 探测时机的状态未必代表最终状态。

- GitHub App 优先打开授权页：不再写死包名去 `setPackage`，
  改用 `queryIntentActivities` 列出所有处理者、排除浏览器后挑出 GitHub App。

---

## [0.0.3] - 2026-09-23

### 修复

- **启动页一闪而过**：splash 的退场条件原来只看「动画播完」，
  导致登录状态到达的时机一有偏差，界面就闪一下或者黑一段。
  现在改为**「动画播完」且「登录状态已知」两个条件都满足**才退场 ——
  无论原生推状态比动画早还是晚，启动页都完整演完 2.2 秒。
- **无障碍设置会砍掉启动页**：`prefers-reduced-motion: reduce` 下
  原来把动画缩到 600ms（一闪而过）。现在保留完整时长，只去掉位移与缩放，
  改成纯透明度变化 —— 这类设置的本意是避免位移/缩放引起不适，不含淡入淡出。

### 新增

- 登录按钮加 **GitHub 官方 Octocat 标识**
  （`github-mark.svg` 的路径，MIT 许可；Material Icons 不含第三方品牌 logo）

### 变更

- 打开 GitHub 授权页时**优先用 GitHub 手机 App**，装不上才退回浏览器

> ⚠️ 已知限制：GitHub App 的包名（`com.github.android`）没在真机上核实过，
> 若唤起失败会自动退回浏览器，不影响登录可用性。

---

## [0.0.2] - 2026-09-23

这一版可以发布了。三件事：个人页、强制登录、应用内更新。

### 新增 · 个人页

- 账户信息区：头像、显示名、`@handle`
- 设置项（沿用 Livolog 的排版，但**排除**它的三个数据项
  —— 存储位置 / 导入数据 / 导出数据）：
  - 语言（English / 中文）
  - 主题（日间 / 夜间 / 跟随系统）
  - 版本（点击手动检查更新）
  - 联系（发邮件）
- 退出登录：破坏性操作，单独一组 + 红色文字 + 二次确认弹窗

### 新增 · GitHub 登录（Device Flow）

- 冷启动流程变成：启动页 → 检查凭据 → 已登录进应用 / 未登录进登录页
- **强制登录**：登录页不提供「稍后再说」
- 登录状态长期保留，只有手动退出登录才失效
- 授权走 GitHub 官方 Device Flow：
  - 网页显示 8 位设备码（点一下即复制），自动打开浏览器去输
  - 不需要 `client_secret`（Device Flow 的设计目的）
  - 申请权限 `repo` + `read:user`
- **token 加密存储**（`EncryptedSharedPreferences` + Android Keystore），
  **不下发到网页层** —— 网页只拿到「是否已登录 / 用户名 / 头像」
- 退出登录**只清凭据，保留阅读数据**（可换账号登录，数据不串）

### 新增 · 应用内更新

- 进入前台自动检查一次 GitHub Release；版本页可手动检查
- 发现新版本 → 弹窗询问 → 下载（带进度）→ 拉起系统安装器
- 四道校验，任何一道不过就拒绝安装：
  - 体积与 Release 声明不一致 → `truncated`
  - 包内版本号与发布标签不一致 → `mismatch`
  - 不比当前装的版本新 → `downgrade`
  - 包结构非法 → `invalid`
- 「上次拉起安装器但没装成」会被识别，下次弹窗多一句权限提示

### 工程

- `gradle.properties` 新增 `GITHUB_CLIENT_ID`（公开值，Device Flow 允许明文）
- 打开 `buildConfig` 以便注入 `GITHUB_CLIENT_ID` / `GITHUB_REPO`
- 新增 `androidx.security:security-crypto:1.1.0`
- 新增 `tools/check_i18n.py`：机械校验所有 `data-i18n` 键都有词条
  （v0.0.2 开发中确实漏过一个，界面上直接显示出了键名）
- 新增 `tools/check_device_flow.py`：实测 GitHub 端点，确认
  `client_id` 有效且 Device Flow 已启用
- 新增 CI：`build.yml`（推送/PR 构建 Debug）、`release.yml`（推标签发布）
- AndroidManifest 增加 `REQUEST_INSTALL_PACKAGES` 权限与 FileProvider

### 已知限制

- **本机没有 Android SDK / JDK，编译未在本地验证过**。所有界面与逻辑
  都在浏览器里实测过，Kotlin 侧只做了静态检查。真正的编译验证依赖 CI。
- 登录的**端到端**流程（真去 GitHub 授权换 token）需要你在真机上走一遍；
  本机只验证到「拿设备码」与「轮询返回 `authorization_pending`」两步。
- 退出登录无法同时撤销 GitHub 侧的授权（Device Flow 拿不到 `client_secret`，
  调不了 revoke 接口）。想彻底撤销要去 GitHub 网站手动删。

---

## [0.0.1] - 2026-09-23

首个版本，不对外发布。搭出网页套壳的骨架。

### 新增

- 启动页动画：Σ 由大到小落定 + 名称淡入上移，全程 CSS 驱动（约 2.2s）
- 禁用安卓原生启动页：主题里把动画图标换成全透明矢量，底色用品牌黑 `#1B1B1B`
- 底部导航栏与三个页面：`Library` / `Ranking` / `Profile`
- 顶部标题栏，英文全大写（与 Livolog 同法）
- 页面内容留空

### 工程

- Kotlin + Gradle KTS，AGP 8.7.3 / Kotlin 2.0.21 / compileSdk 35 / minSdk 26
- WebView 容器：`WebViewAssetLoader` 把 `assets/www/` 挂到
  `https://appassets.androidplatform.net`
- 原生 ↔ 网页双向桥：`ScholariusNative`（网页→原生）、
  `ScholariusShell`（原生→网页）
- 主题：日间 / 夜间两套色板，不用纯黑纯白
- 字体：沿用 Livolog 的更纱等宽黑体子集（Regular + Bold）

### 图标

- Σ 字形，白 → 灰**按累计弧长**渐变（不是对角线线性渐变 ——
  那是对角线投影，Σ 折返时投影不单调，颜色会错位）
- 4 段独立线性渐变拼出弧长效果，相邻段共享折点、端点色相同
- 头尾各一条无渐变短线，尾部截断 2×线宽

---

[0.0.2]: https://github.com/EliasZWC/Scholarius/releases/tag/v0.0.2
[0.0.1]: https://github.com/EliasZWC/Scholarius/releases/tag/v0.0.1
[0.0.3]: https://github.com/EliasZWC/Scholarius/releases/tag/v0.0.3
[0.0.4]: https://github.com/EliasZWC/Scholarius/releases/tag/v0.0.4
[0.0.5]: https://github.com/EliasZWC/Scholarius/releases/tag/v0.0.5
[0.0.6]: https://github.com/EliasZWC/Scholarius/releases/tag/v0.0.6
[0.0.7]: https://github.com/EliasZWC/Scholarius/releases/tag/v0.0.7
[0.0.8]: https://github.com/EliasZWC/Scholarius/releases/tag/v0.0.8
[0.0.9]: https://github.com/EliasZWC/Scholarius/releases/tag/v0.0.9
[0.0.10]: https://github.com/EliasZWC/Scholarius/releases/tag/v0.0.10
[0.0.11]: https://github.com/EliasZWC/Scholarius/releases/tag/v0.0.11
[0.0.12]: https://github.com/EliasZWC/Scholarius/releases/tag/v0.0.12
[0.0.13]: https://github.com/EliasZWC/Scholarius/releases/tag/v0.0.13
[0.0.14]: https://github.com/EliasZWC/Scholarius/releases/tag/v0.0.14
[0.0.15]: https://github.com/EliasZWC/Scholarius/releases/tag/v0.0.15
[0.0.16]: https://github.com/EliasZWC/Scholarius/releases/tag/v0.0.16
[0.0.17]: https://github.com/EliasZWC/Scholarius/releases/tag/v0.0.17
[0.0.18]: https://github.com/EliasZWC/Scholarius/releases/tag/v0.0.18
[0.0.19]: https://github.com/EliasZWC/Scholarius/releases/tag/v0.0.19

<!--
  ⚠️ 0.1.0 – 0.1.4 这五个版本**没有在本文件里留下条目**。

     它们确实发布了（tag 都在），但改动只写进了 commit message。
     补写不划算 —— 那些改动的内容已经沉在 v0.1.5 之前的状态里，
     回头凭 commit 反推一份"看起来完整"的日志只会写进推测，
     反而降低这份文件的可信度。

     所以这里**只补链接**，不补内容 —— 有 tag 就能查 commit 历史。
     若日后有人问「0.1.3 改了什么」，答案在 git log 里，不在本文件。

     ⚠️ 从这里开始，发版必须同步写条目（release.yml 的说明里也要求）。
-->
[0.1.0]: https://github.com/EliasZWC/Scholarius/releases/tag/v0.1.0
[0.1.1]: https://github.com/EliasZWC/Scholarius/releases/tag/v0.1.1
[0.1.2]: https://github.com/EliasZWC/Scholarius/releases/tag/v0.1.2
[0.1.3]: https://github.com/EliasZWC/Scholarius/releases/tag/v0.1.3
[0.1.4]: https://github.com/EliasZWC/Scholarius/releases/tag/v0.1.4
[0.1.5]: https://github.com/EliasZWC/Scholarius/releases/tag/v0.1.5
